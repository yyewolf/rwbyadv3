// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/yyewolf/rwbyadv3/ent/githubstar"
	"github.com/yyewolf/rwbyadv3/ent/player"
)

// GithubStar is the model entity for the GithubStar schema.
type GithubStar struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// PlayerID holds the value of the "player_id" field.
	PlayerID string `json:"player_id,omitempty"`
	// GithubUserID holds the value of the "github_user_id" field.
	GithubUserID string `json:"github_user_id,omitempty"`
	// HasStarred holds the value of the "has_starred" field.
	HasStarred bool `json:"has_starred,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the GithubStarQuery when eager-loading is set.
	Edges        GithubStarEdges `json:"edges"`
	selectValues sql.SelectValues
}

// GithubStarEdges holds the relations/edges for other nodes in the graph.
type GithubStarEdges struct {
	// Of holds the value of the of edge.
	Of *Player `json:"of,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OfOrErr returns the Of value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e GithubStarEdges) OfOrErr() (*Player, error) {
	if e.Of != nil {
		return e.Of, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: player.Label}
	}
	return nil, &NotLoadedError{edge: "of"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GithubStar) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case githubstar.FieldHasStarred:
			values[i] = new(sql.NullBool)
		case githubstar.FieldID:
			values[i] = new(sql.NullInt64)
		case githubstar.FieldPlayerID, githubstar.FieldGithubUserID:
			values[i] = new(sql.NullString)
		case githubstar.FieldCreateTime, githubstar.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GithubStar fields.
func (gs *GithubStar) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case githubstar.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			gs.ID = int(value.Int64)
		case githubstar.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				gs.CreateTime = value.Time
			}
		case githubstar.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				gs.UpdateTime = value.Time
			}
		case githubstar.FieldPlayerID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field player_id", values[i])
			} else if value.Valid {
				gs.PlayerID = value.String
			}
		case githubstar.FieldGithubUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field github_user_id", values[i])
			} else if value.Valid {
				gs.GithubUserID = value.String
			}
		case githubstar.FieldHasStarred:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field has_starred", values[i])
			} else if value.Valid {
				gs.HasStarred = value.Bool
			}
		default:
			gs.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GithubStar.
// This includes values selected through modifiers, order, etc.
func (gs *GithubStar) Value(name string) (ent.Value, error) {
	return gs.selectValues.Get(name)
}

// QueryOf queries the "of" edge of the GithubStar entity.
func (gs *GithubStar) QueryOf() *PlayerQuery {
	return NewGithubStarClient(gs.config).QueryOf(gs)
}

// Update returns a builder for updating this GithubStar.
// Note that you need to call GithubStar.Unwrap() before calling this method if this GithubStar
// was returned from a transaction, and the transaction was committed or rolled back.
func (gs *GithubStar) Update() *GithubStarUpdateOne {
	return NewGithubStarClient(gs.config).UpdateOne(gs)
}

// Unwrap unwraps the GithubStar entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (gs *GithubStar) Unwrap() *GithubStar {
	_tx, ok := gs.config.driver.(*txDriver)
	if !ok {
		panic("ent: GithubStar is not a transactional entity")
	}
	gs.config.driver = _tx.drv
	return gs
}

// String implements the fmt.Stringer.
func (gs *GithubStar) String() string {
	var builder strings.Builder
	builder.WriteString("GithubStar(")
	builder.WriteString(fmt.Sprintf("id=%v, ", gs.ID))
	builder.WriteString("create_time=")
	builder.WriteString(gs.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(gs.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("player_id=")
	builder.WriteString(gs.PlayerID)
	builder.WriteString(", ")
	builder.WriteString("github_user_id=")
	builder.WriteString(gs.GithubUserID)
	builder.WriteString(", ")
	builder.WriteString("has_starred=")
	builder.WriteString(fmt.Sprintf("%v", gs.HasStarred))
	builder.WriteByte(')')
	return builder.String()
}

// GithubStars is a parsable slice of GithubStar.
type GithubStars []*GithubStar
