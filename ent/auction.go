// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yyewolf/rwbyadv3/ent/auction"
	"github.com/yyewolf/rwbyadv3/ent/card"
	"github.com/yyewolf/rwbyadv3/ent/player"
)

// Auction is the model entity for the Auction schema.
type Auction struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty,omitzero"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty,omitzero"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty,omitzero"`
	// PlayerID holds the value of the "player_id" field.
	PlayerID string `json:"player_id,omitempty,omitzero"`
	// CardID holds the value of the "card_id" field.
	CardID uuid.UUID `json:"card_id,omitempty,omitzero"`
	// TimeExtensions holds the value of the "time_extensions" field.
	TimeExtensions int `json:"time_extensions,omitempty,omitzero"`
	// EndsAt holds the value of the "ends_at" field.
	EndsAt time.Time `json:"ends_at,omitempty,omitzero"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AuctionQuery when eager-loading is set.
	Edges        AuctionEdges `json:"edges,omitempty"`
	selectValues sql.SelectValues
}

// AuctionEdges holds the relations/edges for other nodes in the graph.
type AuctionEdges struct {
	// OwnedBy holds the value of the owned_by edge.
	OwnedBy *Player `json:"owned_by,omitempty,omitzero"`
	// Card holds the value of the card edge.
	Card *Card `json:"card,omitempty,omitzero"`
	// Bids holds the value of the bids edge.
	Bids []*AuctionBid `json:"bids,omitempty,omitzero"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// OwnedByOrErr returns the OwnedBy value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AuctionEdges) OwnedByOrErr() (*Player, error) {
	if e.OwnedBy != nil {
		return e.OwnedBy, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: player.Label}
	}
	return nil, &NotLoadedError{edge: "owned_by"}
}

// CardOrErr returns the Card value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AuctionEdges) CardOrErr() (*Card, error) {
	if e.Card != nil {
		return e.Card, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: card.Label}
	}
	return nil, &NotLoadedError{edge: "card"}
}

// BidsOrErr returns the Bids value or an error if the edge
// was not loaded in eager-loading.
func (e AuctionEdges) BidsOrErr() ([]*AuctionBid, error) {
	if e.loadedTypes[2] {
		return e.Bids, nil
	}
	return nil, &NotLoadedError{edge: "bids"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Auction) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case auction.FieldTimeExtensions:
			values[i] = new(sql.NullInt64)
		case auction.FieldPlayerID:
			values[i] = new(sql.NullString)
		case auction.FieldCreateTime, auction.FieldUpdateTime, auction.FieldEndsAt:
			values[i] = new(sql.NullTime)
		case auction.FieldID, auction.FieldCardID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Auction fields.
func (a *Auction) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case auction.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				a.ID = *value
			}
		case auction.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				a.CreateTime = value.Time
			}
		case auction.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				a.UpdateTime = value.Time
			}
		case auction.FieldPlayerID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field player_id", values[i])
			} else if value.Valid {
				a.PlayerID = value.String
			}
		case auction.FieldCardID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field card_id", values[i])
			} else if value != nil {
				a.CardID = *value
			}
		case auction.FieldTimeExtensions:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field time_extensions", values[i])
			} else if value.Valid {
				a.TimeExtensions = int(value.Int64)
			}
		case auction.FieldEndsAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field ends_at", values[i])
			} else if value.Valid {
				a.EndsAt = value.Time
			}
		default:
			a.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Auction.
// This includes values selected through modifiers, order, etc.
func (a *Auction) Value(name string) (ent.Value, error) {
	return a.selectValues.Get(name)
}

// QueryOwnedBy queries the "owned_by" edge of the Auction entity.
func (a *Auction) QueryOwnedBy() *PlayerQuery {
	return NewAuctionClient(a.config).QueryOwnedBy(a)
}

// QueryCard queries the "card" edge of the Auction entity.
func (a *Auction) QueryCard() *CardQuery {
	return NewAuctionClient(a.config).QueryCard(a)
}

// QueryBids queries the "bids" edge of the Auction entity.
func (a *Auction) QueryBids() *AuctionBidQuery {
	return NewAuctionClient(a.config).QueryBids(a)
}

// Update returns a builder for updating this Auction.
// Note that you need to call Auction.Unwrap() before calling this method if this Auction
// was returned from a transaction, and the transaction was committed or rolled back.
func (a *Auction) Update() *AuctionUpdateOne {
	return NewAuctionClient(a.config).UpdateOne(a)
}

// Unwrap unwraps the Auction entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (a *Auction) Unwrap() *Auction {
	_tx, ok := a.config.driver.(*txDriver)
	if !ok {
		panic("ent: Auction is not a transactional entity")
	}
	a.config.driver = _tx.drv
	return a
}

// String implements the fmt.Stringer.
func (a *Auction) String() string {
	var builder strings.Builder
	builder.WriteString("Auction(")
	builder.WriteString(fmt.Sprintf("id=%v, ", a.ID))
	builder.WriteString("create_time=")
	builder.WriteString(a.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(a.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("player_id=")
	builder.WriteString(a.PlayerID)
	builder.WriteString(", ")
	builder.WriteString("card_id=")
	builder.WriteString(fmt.Sprintf("%v", a.CardID))
	builder.WriteString(", ")
	builder.WriteString("time_extensions=")
	builder.WriteString(fmt.Sprintf("%v", a.TimeExtensions))
	builder.WriteString(", ")
	builder.WriteString("ends_at=")
	builder.WriteString(a.EndsAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Auctions is a parsable slice of Auction.
type Auctions []*Auction
