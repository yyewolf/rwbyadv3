// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yyewolf/rwbyadv3/ent/job"
)

// Job is the model entity for the Job schema.
type Job struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty,omitzero"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty,omitzero"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty,omitzero"`
	// Jobkey holds the value of the "jobkey" field.
	Jobkey string `json:"jobkey,omitempty,omitzero"`
	// Retries holds the value of the "retries" field.
	Retries int `json:"retries,omitempty,omitzero"`
	// RunAt holds the value of the "run_at" field.
	RunAt time.Time `json:"run_at,omitempty,omitzero"`
	// Params holds the value of the "params" field.
	Params map[string]interface{} `json:"params,omitempty,omitzero"`
	// LastRunID holds the value of the "last_run_id" field.
	LastRunID int64 `json:"last_run_id,omitempty,omitzero"`
	// Recurring holds the value of the "recurring" field.
	Recurring bool `json:"recurring,omitempty,omitzero"`
	// DeltaTime holds the value of the "delta_time" field.
	DeltaTime int64 `json:"delta_time,omitempty,omitzero"`
	// Errored holds the value of the "errored" field.
	Errored      bool `json:"errored,omitempty,omitzero"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Job) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case job.FieldParams:
			values[i] = new([]byte)
		case job.FieldRecurring, job.FieldErrored:
			values[i] = new(sql.NullBool)
		case job.FieldRetries, job.FieldLastRunID, job.FieldDeltaTime:
			values[i] = new(sql.NullInt64)
		case job.FieldJobkey:
			values[i] = new(sql.NullString)
		case job.FieldCreateTime, job.FieldUpdateTime, job.FieldRunAt:
			values[i] = new(sql.NullTime)
		case job.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Job fields.
func (j *Job) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case job.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				j.ID = *value
			}
		case job.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				j.CreateTime = value.Time
			}
		case job.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				j.UpdateTime = value.Time
			}
		case job.FieldJobkey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field jobkey", values[i])
			} else if value.Valid {
				j.Jobkey = value.String
			}
		case job.FieldRetries:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field retries", values[i])
			} else if value.Valid {
				j.Retries = int(value.Int64)
			}
		case job.FieldRunAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field run_at", values[i])
			} else if value.Valid {
				j.RunAt = value.Time
			}
		case job.FieldParams:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field params", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &j.Params); err != nil {
					return fmt.Errorf("unmarshal field params: %w", err)
				}
			}
		case job.FieldLastRunID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field last_run_id", values[i])
			} else if value.Valid {
				j.LastRunID = value.Int64
			}
		case job.FieldRecurring:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field recurring", values[i])
			} else if value.Valid {
				j.Recurring = value.Bool
			}
		case job.FieldDeltaTime:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field delta_time", values[i])
			} else if value.Valid {
				j.DeltaTime = value.Int64
			}
		case job.FieldErrored:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field errored", values[i])
			} else if value.Valid {
				j.Errored = value.Bool
			}
		default:
			j.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Job.
// This includes values selected through modifiers, order, etc.
func (j *Job) Value(name string) (ent.Value, error) {
	return j.selectValues.Get(name)
}

// Update returns a builder for updating this Job.
// Note that you need to call Job.Unwrap() before calling this method if this Job
// was returned from a transaction, and the transaction was committed or rolled back.
func (j *Job) Update() *JobUpdateOne {
	return NewJobClient(j.config).UpdateOne(j)
}

// Unwrap unwraps the Job entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (j *Job) Unwrap() *Job {
	_tx, ok := j.config.driver.(*txDriver)
	if !ok {
		panic("ent: Job is not a transactional entity")
	}
	j.config.driver = _tx.drv
	return j
}

// String implements the fmt.Stringer.
func (j *Job) String() string {
	var builder strings.Builder
	builder.WriteString("Job(")
	builder.WriteString(fmt.Sprintf("id=%v, ", j.ID))
	builder.WriteString("create_time=")
	builder.WriteString(j.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(j.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("jobkey=")
	builder.WriteString(j.Jobkey)
	builder.WriteString(", ")
	builder.WriteString("retries=")
	builder.WriteString(fmt.Sprintf("%v", j.Retries))
	builder.WriteString(", ")
	builder.WriteString("run_at=")
	builder.WriteString(j.RunAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("params=")
	builder.WriteString(fmt.Sprintf("%v", j.Params))
	builder.WriteString(", ")
	builder.WriteString("last_run_id=")
	builder.WriteString(fmt.Sprintf("%v", j.LastRunID))
	builder.WriteString(", ")
	builder.WriteString("recurring=")
	builder.WriteString(fmt.Sprintf("%v", j.Recurring))
	builder.WriteString(", ")
	builder.WriteString("delta_time=")
	builder.WriteString(fmt.Sprintf("%v", j.DeltaTime))
	builder.WriteString(", ")
	builder.WriteString("errored=")
	builder.WriteString(fmt.Sprintf("%v", j.Errored))
	builder.WriteByte(')')
	return builder.String()
}

// Jobs is a parsable slice of Job.
type Jobs []*Job
