// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/yyewolf/rwbyadv3/ent/cookie"
	"github.com/yyewolf/rwbyadv3/ent/player"
)

// CookieCreate is the builder for creating a Cookie entity.
type CookieCreate struct {
	config
	mutation *CookieMutation
	hooks    []Hook
	conflict []sql.ConflictOption
}

// SetCreateTime sets the "create_time" field.
func (cc *CookieCreate) SetCreateTime(t time.Time) *CookieCreate {
	cc.mutation.SetCreateTime(t)
	return cc
}

// SetNillableCreateTime sets the "create_time" field if the given value is not nil.
func (cc *CookieCreate) SetNillableCreateTime(t *time.Time) *CookieCreate {
	if t != nil {
		cc.SetCreateTime(*t)
	}
	return cc
}

// SetUpdateTime sets the "update_time" field.
func (cc *CookieCreate) SetUpdateTime(t time.Time) *CookieCreate {
	cc.mutation.SetUpdateTime(t)
	return cc
}

// SetNillableUpdateTime sets the "update_time" field if the given value is not nil.
func (cc *CookieCreate) SetNillableUpdateTime(t *time.Time) *CookieCreate {
	if t != nil {
		cc.SetUpdateTime(*t)
	}
	return cc
}

// SetPlayerID sets the "player_id" field.
func (cc *CookieCreate) SetPlayerID(s string) *CookieCreate {
	cc.mutation.SetPlayerID(s)
	return cc
}

// SetExpiresAt sets the "expires_at" field.
func (cc *CookieCreate) SetExpiresAt(t time.Time) *CookieCreate {
	cc.mutation.SetExpiresAt(t)
	return cc
}

// SetID sets the "id" field.
func (cc *CookieCreate) SetID(u uuid.UUID) *CookieCreate {
	cc.mutation.SetID(u)
	return cc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (cc *CookieCreate) SetNillableID(u *uuid.UUID) *CookieCreate {
	if u != nil {
		cc.SetID(*u)
	}
	return cc
}

// SetPlayer sets the "player" edge to the Player entity.
func (cc *CookieCreate) SetPlayer(p *Player) *CookieCreate {
	return cc.SetPlayerID(p.ID)
}

// Mutation returns the CookieMutation object of the builder.
func (cc *CookieCreate) Mutation() *CookieMutation {
	return cc.mutation
}

// Save creates the Cookie in the database.
func (cc *CookieCreate) Save(ctx context.Context) (*Cookie, error) {
	cc.defaults()
	return withHooks(ctx, cc.sqlSave, cc.mutation, cc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (cc *CookieCreate) SaveX(ctx context.Context) *Cookie {
	v, err := cc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (cc *CookieCreate) Exec(ctx context.Context) error {
	_, err := cc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (cc *CookieCreate) ExecX(ctx context.Context) {
	if err := cc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (cc *CookieCreate) defaults() {
	if _, ok := cc.mutation.CreateTime(); !ok {
		v := cookie.DefaultCreateTime()
		cc.mutation.SetCreateTime(v)
	}
	if _, ok := cc.mutation.UpdateTime(); !ok {
		v := cookie.DefaultUpdateTime()
		cc.mutation.SetUpdateTime(v)
	}
	if _, ok := cc.mutation.ID(); !ok {
		v := cookie.DefaultID()
		cc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (cc *CookieCreate) check() error {
	if _, ok := cc.mutation.CreateTime(); !ok {
		return &ValidationError{Name: "create_time", err: errors.New(`ent: missing required field "Cookie.create_time"`)}
	}
	if _, ok := cc.mutation.UpdateTime(); !ok {
		return &ValidationError{Name: "update_time", err: errors.New(`ent: missing required field "Cookie.update_time"`)}
	}
	if _, ok := cc.mutation.PlayerID(); !ok {
		return &ValidationError{Name: "player_id", err: errors.New(`ent: missing required field "Cookie.player_id"`)}
	}
	if _, ok := cc.mutation.ExpiresAt(); !ok {
		return &ValidationError{Name: "expires_at", err: errors.New(`ent: missing required field "Cookie.expires_at"`)}
	}
	if len(cc.mutation.PlayerIDs()) == 0 {
		return &ValidationError{Name: "player", err: errors.New(`ent: missing required edge "Cookie.player"`)}
	}
	return nil
}

func (cc *CookieCreate) sqlSave(ctx context.Context) (*Cookie, error) {
	if err := cc.check(); err != nil {
		return nil, err
	}
	_node, _spec := cc.createSpec()
	if err := sqlgraph.CreateNode(ctx, cc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	cc.mutation.id = &_node.ID
	cc.mutation.done = true
	return _node, nil
}

func (cc *CookieCreate) createSpec() (*Cookie, *sqlgraph.CreateSpec) {
	var (
		_node = &Cookie{config: cc.config}
		_spec = sqlgraph.NewCreateSpec(cookie.Table, sqlgraph.NewFieldSpec(cookie.FieldID, field.TypeUUID))
	)
	_spec.OnConflict = cc.conflict
	if id, ok := cc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := cc.mutation.CreateTime(); ok {
		_spec.SetField(cookie.FieldCreateTime, field.TypeTime, value)
		_node.CreateTime = value
	}
	if value, ok := cc.mutation.UpdateTime(); ok {
		_spec.SetField(cookie.FieldUpdateTime, field.TypeTime, value)
		_node.UpdateTime = value
	}
	if value, ok := cc.mutation.ExpiresAt(); ok {
		_spec.SetField(cookie.FieldExpiresAt, field.TypeTime, value)
		_node.ExpiresAt = value
	}
	if nodes := cc.mutation.PlayerIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   cookie.PlayerTable,
			Columns: []string{cookie.PlayerColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(player.FieldID, field.TypeString),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.PlayerID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.Cookie.Create().
//		SetCreateTime(v).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.CookieUpsert) {
//			SetCreateTime(v+v).
//		}).
//		Exec(ctx)
func (cc *CookieCreate) OnConflict(opts ...sql.ConflictOption) *CookieUpsertOne {
	cc.conflict = opts
	return &CookieUpsertOne{
		create: cc,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.Cookie.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (cc *CookieCreate) OnConflictColumns(columns ...string) *CookieUpsertOne {
	cc.conflict = append(cc.conflict, sql.ConflictColumns(columns...))
	return &CookieUpsertOne{
		create: cc,
	}
}

type (
	// CookieUpsertOne is the builder for "upsert"-ing
	//  one Cookie node.
	CookieUpsertOne struct {
		create *CookieCreate
	}

	// CookieUpsert is the "OnConflict" setter.
	CookieUpsert struct {
		*sql.UpdateSet
	}
)

// SetUpdateTime sets the "update_time" field.
func (u *CookieUpsert) SetUpdateTime(v time.Time) *CookieUpsert {
	u.Set(cookie.FieldUpdateTime, v)
	return u
}

// UpdateUpdateTime sets the "update_time" field to the value that was provided on create.
func (u *CookieUpsert) UpdateUpdateTime() *CookieUpsert {
	u.SetExcluded(cookie.FieldUpdateTime)
	return u
}

// SetPlayerID sets the "player_id" field.
func (u *CookieUpsert) SetPlayerID(v string) *CookieUpsert {
	u.Set(cookie.FieldPlayerID, v)
	return u
}

// UpdatePlayerID sets the "player_id" field to the value that was provided on create.
func (u *CookieUpsert) UpdatePlayerID() *CookieUpsert {
	u.SetExcluded(cookie.FieldPlayerID)
	return u
}

// SetExpiresAt sets the "expires_at" field.
func (u *CookieUpsert) SetExpiresAt(v time.Time) *CookieUpsert {
	u.Set(cookie.FieldExpiresAt, v)
	return u
}

// UpdateExpiresAt sets the "expires_at" field to the value that was provided on create.
func (u *CookieUpsert) UpdateExpiresAt() *CookieUpsert {
	u.SetExcluded(cookie.FieldExpiresAt)
	return u
}

// UpdateNewValues updates the mutable fields using the new values that were set on create except the ID field.
// Using this option is equivalent to using:
//
//	client.Cookie.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(cookie.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *CookieUpsertOne) UpdateNewValues() *CookieUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		if _, exists := u.create.mutation.ID(); exists {
			s.SetIgnore(cookie.FieldID)
		}
		if _, exists := u.create.mutation.CreateTime(); exists {
			s.SetIgnore(cookie.FieldCreateTime)
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.Cookie.Create().
//	    OnConflict(sql.ResolveWithIgnore()).
//	    Exec(ctx)
func (u *CookieUpsertOne) Ignore() *CookieUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *CookieUpsertOne) DoNothing() *CookieUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the CookieCreate.OnConflict
// documentation for more info.
func (u *CookieUpsertOne) Update(set func(*CookieUpsert)) *CookieUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&CookieUpsert{UpdateSet: update})
	}))
	return u
}

// SetUpdateTime sets the "update_time" field.
func (u *CookieUpsertOne) SetUpdateTime(v time.Time) *CookieUpsertOne {
	return u.Update(func(s *CookieUpsert) {
		s.SetUpdateTime(v)
	})
}

// UpdateUpdateTime sets the "update_time" field to the value that was provided on create.
func (u *CookieUpsertOne) UpdateUpdateTime() *CookieUpsertOne {
	return u.Update(func(s *CookieUpsert) {
		s.UpdateUpdateTime()
	})
}

// SetPlayerID sets the "player_id" field.
func (u *CookieUpsertOne) SetPlayerID(v string) *CookieUpsertOne {
	return u.Update(func(s *CookieUpsert) {
		s.SetPlayerID(v)
	})
}

// UpdatePlayerID sets the "player_id" field to the value that was provided on create.
func (u *CookieUpsertOne) UpdatePlayerID() *CookieUpsertOne {
	return u.Update(func(s *CookieUpsert) {
		s.UpdatePlayerID()
	})
}

// SetExpiresAt sets the "expires_at" field.
func (u *CookieUpsertOne) SetExpiresAt(v time.Time) *CookieUpsertOne {
	return u.Update(func(s *CookieUpsert) {
		s.SetExpiresAt(v)
	})
}

// UpdateExpiresAt sets the "expires_at" field to the value that was provided on create.
func (u *CookieUpsertOne) UpdateExpiresAt() *CookieUpsertOne {
	return u.Update(func(s *CookieUpsert) {
		s.UpdateExpiresAt()
	})
}

// Exec executes the query.
func (u *CookieUpsertOne) Exec(ctx context.Context) error {
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for CookieCreate.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *CookieUpsertOne) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}

// Exec executes the UPSERT query and returns the inserted/updated ID.
func (u *CookieUpsertOne) ID(ctx context.Context) (id uuid.UUID, err error) {
	if u.create.driver.Dialect() == dialect.MySQL {
		// In case of "ON CONFLICT", there is no way to get back non-numeric ID
		// fields from the database since MySQL does not support the RETURNING clause.
		return id, errors.New("ent: CookieUpsertOne.ID is not supported by MySQL driver. Use CookieUpsertOne.Exec instead")
	}
	node, err := u.create.Save(ctx)
	if err != nil {
		return id, err
	}
	return node.ID, nil
}

// IDX is like ID, but panics if an error occurs.
func (u *CookieUpsertOne) IDX(ctx context.Context) uuid.UUID {
	id, err := u.ID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// CookieCreateBulk is the builder for creating many Cookie entities in bulk.
type CookieCreateBulk struct {
	config
	err      error
	builders []*CookieCreate
	conflict []sql.ConflictOption
}

// Save creates the Cookie entities in the database.
func (ccb *CookieCreateBulk) Save(ctx context.Context) ([]*Cookie, error) {
	if ccb.err != nil {
		return nil, ccb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(ccb.builders))
	nodes := make([]*Cookie, len(ccb.builders))
	mutators := make([]Mutator, len(ccb.builders))
	for i := range ccb.builders {
		func(i int, root context.Context) {
			builder := ccb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*CookieMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ccb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					spec.OnConflict = ccb.conflict
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ccb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ccb *CookieCreateBulk) SaveX(ctx context.Context) []*Cookie {
	v, err := ccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ccb *CookieCreateBulk) Exec(ctx context.Context) error {
	_, err := ccb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ccb *CookieCreateBulk) ExecX(ctx context.Context) {
	if err := ccb.Exec(ctx); err != nil {
		panic(err)
	}
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.Cookie.CreateBulk(builders...).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.CookieUpsert) {
//			SetCreateTime(v+v).
//		}).
//		Exec(ctx)
func (ccb *CookieCreateBulk) OnConflict(opts ...sql.ConflictOption) *CookieUpsertBulk {
	ccb.conflict = opts
	return &CookieUpsertBulk{
		create: ccb,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.Cookie.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (ccb *CookieCreateBulk) OnConflictColumns(columns ...string) *CookieUpsertBulk {
	ccb.conflict = append(ccb.conflict, sql.ConflictColumns(columns...))
	return &CookieUpsertBulk{
		create: ccb,
	}
}

// CookieUpsertBulk is the builder for "upsert"-ing
// a bulk of Cookie nodes.
type CookieUpsertBulk struct {
	create *CookieCreateBulk
}

// UpdateNewValues updates the mutable fields using the new values that
// were set on create. Using this option is equivalent to using:
//
//	client.Cookie.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(cookie.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *CookieUpsertBulk) UpdateNewValues() *CookieUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		for _, b := range u.create.builders {
			if _, exists := b.mutation.ID(); exists {
				s.SetIgnore(cookie.FieldID)
			}
			if _, exists := b.mutation.CreateTime(); exists {
				s.SetIgnore(cookie.FieldCreateTime)
			}
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.Cookie.Create().
//		OnConflict(sql.ResolveWithIgnore()).
//		Exec(ctx)
func (u *CookieUpsertBulk) Ignore() *CookieUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *CookieUpsertBulk) DoNothing() *CookieUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the CookieCreateBulk.OnConflict
// documentation for more info.
func (u *CookieUpsertBulk) Update(set func(*CookieUpsert)) *CookieUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&CookieUpsert{UpdateSet: update})
	}))
	return u
}

// SetUpdateTime sets the "update_time" field.
func (u *CookieUpsertBulk) SetUpdateTime(v time.Time) *CookieUpsertBulk {
	return u.Update(func(s *CookieUpsert) {
		s.SetUpdateTime(v)
	})
}

// UpdateUpdateTime sets the "update_time" field to the value that was provided on create.
func (u *CookieUpsertBulk) UpdateUpdateTime() *CookieUpsertBulk {
	return u.Update(func(s *CookieUpsert) {
		s.UpdateUpdateTime()
	})
}

// SetPlayerID sets the "player_id" field.
func (u *CookieUpsertBulk) SetPlayerID(v string) *CookieUpsertBulk {
	return u.Update(func(s *CookieUpsert) {
		s.SetPlayerID(v)
	})
}

// UpdatePlayerID sets the "player_id" field to the value that was provided on create.
func (u *CookieUpsertBulk) UpdatePlayerID() *CookieUpsertBulk {
	return u.Update(func(s *CookieUpsert) {
		s.UpdatePlayerID()
	})
}

// SetExpiresAt sets the "expires_at" field.
func (u *CookieUpsertBulk) SetExpiresAt(v time.Time) *CookieUpsertBulk {
	return u.Update(func(s *CookieUpsert) {
		s.SetExpiresAt(v)
	})
}

// UpdateExpiresAt sets the "expires_at" field to the value that was provided on create.
func (u *CookieUpsertBulk) UpdateExpiresAt() *CookieUpsertBulk {
	return u.Update(func(s *CookieUpsert) {
		s.UpdateExpiresAt()
	})
}

// Exec executes the query.
func (u *CookieUpsertBulk) Exec(ctx context.Context) error {
	if u.create.err != nil {
		return u.create.err
	}
	for i, b := range u.create.builders {
		if len(b.conflict) != 0 {
			return fmt.Errorf("ent: OnConflict was set for builder %d. Set it on the CookieCreateBulk instead", i)
		}
	}
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for CookieCreateBulk.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *CookieUpsertBulk) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}
