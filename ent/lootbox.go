// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yyewolf/rwbyadv3/ent/lootbox"
	"github.com/yyewolf/rwbyadv3/ent/player"
	"github.com/yyewolf/rwbyadv3/ent/schema/enums"
)

// LootBox is the model entity for the LootBox schema.
type LootBox struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty,omitzero"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty,omitzero"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty,omitzero"`
	// PlayerID holds the value of the "player_id" field.
	PlayerID string `json:"player_id,omitempty,omitzero"`
	// Type holds the value of the "type" field.
	Type enums.LootBoxType `json:"type,omitempty,omitzero"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the LootBoxQuery when eager-loading is set.
	Edges        LootBoxEdges `json:"edges,omitempty,omitzero"`
	selectValues sql.SelectValues
}

// LootBoxEdges holds the relations/edges for other nodes in the graph.
type LootBoxEdges struct {
	// OwnedBy holds the value of the owned_by edge.
	OwnedBy *Player `json:"owned_by,omitempty,omitzero"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OwnedByOrErr returns the OwnedBy value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LootBoxEdges) OwnedByOrErr() (*Player, error) {
	if e.OwnedBy != nil {
		return e.OwnedBy, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: player.Label}
	}
	return nil, &NotLoadedError{edge: "owned_by"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LootBox) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case lootbox.FieldPlayerID, lootbox.FieldType:
			values[i] = new(sql.NullString)
		case lootbox.FieldCreateTime, lootbox.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case lootbox.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LootBox fields.
func (lb *LootBox) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case lootbox.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				lb.ID = *value
			}
		case lootbox.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				lb.CreateTime = value.Time
			}
		case lootbox.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				lb.UpdateTime = value.Time
			}
		case lootbox.FieldPlayerID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field player_id", values[i])
			} else if value.Valid {
				lb.PlayerID = value.String
			}
		case lootbox.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				lb.Type = enums.LootBoxType(value.String)
			}
		default:
			lb.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the LootBox.
// This includes values selected through modifiers, order, etc.
func (lb *LootBox) Value(name string) (ent.Value, error) {
	return lb.selectValues.Get(name)
}

// QueryOwnedBy queries the "owned_by" edge of the LootBox entity.
func (lb *LootBox) QueryOwnedBy() *PlayerQuery {
	return NewLootBoxClient(lb.config).QueryOwnedBy(lb)
}

// Update returns a builder for updating this LootBox.
// Note that you need to call LootBox.Unwrap() before calling this method if this LootBox
// was returned from a transaction, and the transaction was committed or rolled back.
func (lb *LootBox) Update() *LootBoxUpdateOne {
	return NewLootBoxClient(lb.config).UpdateOne(lb)
}

// Unwrap unwraps the LootBox entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (lb *LootBox) Unwrap() *LootBox {
	_tx, ok := lb.config.driver.(*txDriver)
	if !ok {
		panic("ent: LootBox is not a transactional entity")
	}
	lb.config.driver = _tx.drv
	return lb
}

// String implements the fmt.Stringer.
func (lb *LootBox) String() string {
	var builder strings.Builder
	builder.WriteString("LootBox(")
	builder.WriteString(fmt.Sprintf("id=%v, ", lb.ID))
	builder.WriteString("create_time=")
	builder.WriteString(lb.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(lb.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("player_id=")
	builder.WriteString(lb.PlayerID)
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", lb.Type))
	builder.WriteByte(')')
	return builder.String()
}

// LootBoxes is a parsable slice of LootBox.
type LootBoxes []*LootBox
