// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/yyewolf/rwbyadv3/ent/player"
	"github.com/yyewolf/rwbyadv3/ent/playerlimit"
)

// PlayerLimit is the model entity for the PlayerLimit schema.
type PlayerLimit struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty,omitzero"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty,omitzero"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty,omitzero"`
	// PlayerID holds the value of the "player_id" field.
	PlayerID string `json:"player_id,omitempty,omitzero"`
	// DungeonsLeft holds the value of the "dungeons_left" field.
	DungeonsLeft int `json:"dungeons_left,omitempty,omitzero"`
	// DungeonsResetAt holds the value of the "dungeons_reset_at" field.
	DungeonsResetAt time.Time `json:"dungeons_reset_at,omitempty,omitzero"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PlayerLimitQuery when eager-loading is set.
	Edges        PlayerLimitEdges `json:"edges,omitempty,omitzero"`
	selectValues sql.SelectValues
}

// PlayerLimitEdges holds the relations/edges for other nodes in the graph.
type PlayerLimitEdges struct {
	// Of holds the value of the of edge.
	Of *Player `json:"of,omitempty,omitzero"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OfOrErr returns the Of value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PlayerLimitEdges) OfOrErr() (*Player, error) {
	if e.Of != nil {
		return e.Of, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: player.Label}
	}
	return nil, &NotLoadedError{edge: "of"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PlayerLimit) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case playerlimit.FieldID, playerlimit.FieldDungeonsLeft:
			values[i] = new(sql.NullInt64)
		case playerlimit.FieldPlayerID:
			values[i] = new(sql.NullString)
		case playerlimit.FieldCreateTime, playerlimit.FieldUpdateTime, playerlimit.FieldDungeonsResetAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PlayerLimit fields.
func (pl *PlayerLimit) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case playerlimit.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pl.ID = int(value.Int64)
		case playerlimit.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				pl.CreateTime = value.Time
			}
		case playerlimit.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				pl.UpdateTime = value.Time
			}
		case playerlimit.FieldPlayerID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field player_id", values[i])
			} else if value.Valid {
				pl.PlayerID = value.String
			}
		case playerlimit.FieldDungeonsLeft:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field dungeons_left", values[i])
			} else if value.Valid {
				pl.DungeonsLeft = int(value.Int64)
			}
		case playerlimit.FieldDungeonsResetAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field dungeons_reset_at", values[i])
			} else if value.Valid {
				pl.DungeonsResetAt = value.Time
			}
		default:
			pl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PlayerLimit.
// This includes values selected through modifiers, order, etc.
func (pl *PlayerLimit) Value(name string) (ent.Value, error) {
	return pl.selectValues.Get(name)
}

// QueryOf queries the "of" edge of the PlayerLimit entity.
func (pl *PlayerLimit) QueryOf() *PlayerQuery {
	return NewPlayerLimitClient(pl.config).QueryOf(pl)
}

// Update returns a builder for updating this PlayerLimit.
// Note that you need to call PlayerLimit.Unwrap() before calling this method if this PlayerLimit
// was returned from a transaction, and the transaction was committed or rolled back.
func (pl *PlayerLimit) Update() *PlayerLimitUpdateOne {
	return NewPlayerLimitClient(pl.config).UpdateOne(pl)
}

// Unwrap unwraps the PlayerLimit entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pl *PlayerLimit) Unwrap() *PlayerLimit {
	_tx, ok := pl.config.driver.(*txDriver)
	if !ok {
		panic("ent: PlayerLimit is not a transactional entity")
	}
	pl.config.driver = _tx.drv
	return pl
}

// String implements the fmt.Stringer.
func (pl *PlayerLimit) String() string {
	var builder strings.Builder
	builder.WriteString("PlayerLimit(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pl.ID))
	builder.WriteString("create_time=")
	builder.WriteString(pl.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(pl.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("player_id=")
	builder.WriteString(pl.PlayerID)
	builder.WriteString(", ")
	builder.WriteString("dungeons_left=")
	builder.WriteString(fmt.Sprintf("%v", pl.DungeonsLeft))
	builder.WriteString(", ")
	builder.WriteString("dungeons_reset_at=")
	builder.WriteString(pl.DungeonsResetAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PlayerLimits is a parsable slice of PlayerLimit.
type PlayerLimits []*PlayerLimit
