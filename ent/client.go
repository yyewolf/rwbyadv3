// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"log"
	"reflect"

	"github.com/google/uuid"
	"github.com/yyewolf/rwbyadv3/ent/migrate"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"github.com/yyewolf/rwbyadv3/ent/auction"
	"github.com/yyewolf/rwbyadv3/ent/auctionbid"
	"github.com/yyewolf/rwbyadv3/ent/authstate"
	"github.com/yyewolf/rwbyadv3/ent/card"
	"github.com/yyewolf/rwbyadv3/ent/cardstats"
	"github.com/yyewolf/rwbyadv3/ent/cardtype"
	"github.com/yyewolf/rwbyadv3/ent/cookie"
	"github.com/yyewolf/rwbyadv3/ent/daily"
	"github.com/yyewolf/rwbyadv3/ent/dungeon"
	"github.com/yyewolf/rwbyadv3/ent/githubstar"
	"github.com/yyewolf/rwbyadv3/ent/job"
	"github.com/yyewolf/rwbyadv3/ent/listing"
	"github.com/yyewolf/rwbyadv3/ent/lootbox"
	"github.com/yyewolf/rwbyadv3/ent/player"
	"github.com/yyewolf/rwbyadv3/ent/playerdeck"
	"github.com/yyewolf/rwbyadv3/ent/playerfavoritecards"
	"github.com/yyewolf/rwbyadv3/ent/playerlimit"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// Auction is the client for interacting with the Auction builders.
	Auction *AuctionClient
	// AuctionBid is the client for interacting with the AuctionBid builders.
	AuctionBid *AuctionBidClient
	// AuthState is the client for interacting with the AuthState builders.
	AuthState *AuthStateClient
	// Card is the client for interacting with the Card builders.
	Card *CardClient
	// CardStats is the client for interacting with the CardStats builders.
	CardStats *CardStatsClient
	// CardType is the client for interacting with the CardType builders.
	CardType *CardTypeClient
	// Cookie is the client for interacting with the Cookie builders.
	Cookie *CookieClient
	// Daily is the client for interacting with the Daily builders.
	Daily *DailyClient
	// Dungeon is the client for interacting with the Dungeon builders.
	Dungeon *DungeonClient
	// GithubStar is the client for interacting with the GithubStar builders.
	GithubStar *GithubStarClient
	// Job is the client for interacting with the Job builders.
	Job *JobClient
	// Listing is the client for interacting with the Listing builders.
	Listing *ListingClient
	// LootBox is the client for interacting with the LootBox builders.
	LootBox *LootBoxClient
	// Player is the client for interacting with the Player builders.
	Player *PlayerClient
	// PlayerDeck is the client for interacting with the PlayerDeck builders.
	PlayerDeck *PlayerDeckClient
	// PlayerFavoriteCards is the client for interacting with the PlayerFavoriteCards builders.
	PlayerFavoriteCards *PlayerFavoriteCardsClient
	// PlayerLimit is the client for interacting with the PlayerLimit builders.
	PlayerLimit *PlayerLimitClient
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	client := &Client{config: newConfig(opts...)}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.Auction = NewAuctionClient(c.config)
	c.AuctionBid = NewAuctionBidClient(c.config)
	c.AuthState = NewAuthStateClient(c.config)
	c.Card = NewCardClient(c.config)
	c.CardStats = NewCardStatsClient(c.config)
	c.CardType = NewCardTypeClient(c.config)
	c.Cookie = NewCookieClient(c.config)
	c.Daily = NewDailyClient(c.config)
	c.Dungeon = NewDungeonClient(c.config)
	c.GithubStar = NewGithubStarClient(c.config)
	c.Job = NewJobClient(c.config)
	c.Listing = NewListingClient(c.config)
	c.LootBox = NewLootBoxClient(c.config)
	c.Player = NewPlayerClient(c.config)
	c.PlayerDeck = NewPlayerDeckClient(c.config)
	c.PlayerFavoriteCards = NewPlayerFavoriteCardsClient(c.config)
	c.PlayerLimit = NewPlayerLimitClient(c.config)
}

type (
	// config is the configuration for the client and its builder.
	config struct {
		// driver used for executing database requests.
		driver dialect.Driver
		// debug enable a debug logging.
		debug bool
		// log used for logging on debug mode.
		log func(...any)
		// hooks to execute on mutations.
		hooks *hooks
		// interceptors to execute on queries.
		inters *inters
	}
	// Option function to configure the client.
	Option func(*config)
)

// newConfig creates a new config for the client.
func newConfig(opts ...Option) config {
	cfg := config{log: log.Println, hooks: &hooks{}, inters: &inters{}}
	cfg.options(opts...)
	return cfg
}

// options applies the options on the config object.
func (c *config) options(opts ...Option) {
	for _, opt := range opts {
		opt(c)
	}
	if c.debug {
		c.driver = dialect.Debug(c.driver, c.log)
	}
}

// Debug enables debug logging on the ent.Driver.
func Debug() Option {
	return func(c *config) {
		c.debug = true
	}
}

// Log sets the logging function for debug mode.
func Log(fn func(...any)) Option {
	return func(c *config) {
		c.log = fn
	}
}

// Driver configures the client driver.
func Driver(driver dialect.Driver) Option {
	return func(c *config) {
		c.driver = driver
	}
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// ErrTxStarted is returned when trying to start a new transaction from a transactional client.
var ErrTxStarted = errors.New("ent: cannot start a transaction within a transaction")

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, ErrTxStarted
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:                 ctx,
		config:              cfg,
		Auction:             NewAuctionClient(cfg),
		AuctionBid:          NewAuctionBidClient(cfg),
		AuthState:           NewAuthStateClient(cfg),
		Card:                NewCardClient(cfg),
		CardStats:           NewCardStatsClient(cfg),
		CardType:            NewCardTypeClient(cfg),
		Cookie:              NewCookieClient(cfg),
		Daily:               NewDailyClient(cfg),
		Dungeon:             NewDungeonClient(cfg),
		GithubStar:          NewGithubStarClient(cfg),
		Job:                 NewJobClient(cfg),
		Listing:             NewListingClient(cfg),
		LootBox:             NewLootBoxClient(cfg),
		Player:              NewPlayerClient(cfg),
		PlayerDeck:          NewPlayerDeckClient(cfg),
		PlayerFavoriteCards: NewPlayerFavoriteCardsClient(cfg),
		PlayerLimit:         NewPlayerLimitClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, errors.New("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:                 ctx,
		config:              cfg,
		Auction:             NewAuctionClient(cfg),
		AuctionBid:          NewAuctionBidClient(cfg),
		AuthState:           NewAuthStateClient(cfg),
		Card:                NewCardClient(cfg),
		CardStats:           NewCardStatsClient(cfg),
		CardType:            NewCardTypeClient(cfg),
		Cookie:              NewCookieClient(cfg),
		Daily:               NewDailyClient(cfg),
		Dungeon:             NewDungeonClient(cfg),
		GithubStar:          NewGithubStarClient(cfg),
		Job:                 NewJobClient(cfg),
		Listing:             NewListingClient(cfg),
		LootBox:             NewLootBoxClient(cfg),
		Player:              NewPlayerClient(cfg),
		PlayerDeck:          NewPlayerDeckClient(cfg),
		PlayerFavoriteCards: NewPlayerFavoriteCardsClient(cfg),
		PlayerLimit:         NewPlayerLimitClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		Auction.
//		Query().
//		Count(ctx)
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	for _, n := range []interface{ Use(...Hook) }{
		c.Auction, c.AuctionBid, c.AuthState, c.Card, c.CardStats, c.CardType, c.Cookie,
		c.Daily, c.Dungeon, c.GithubStar, c.Job, c.Listing, c.LootBox, c.Player,
		c.PlayerDeck, c.PlayerFavoriteCards, c.PlayerLimit,
	} {
		n.Use(hooks...)
	}
}

// Intercept adds the query interceptors to all the entity clients.
// In order to add interceptors to a specific client, call: `client.Node.Intercept(...)`.
func (c *Client) Intercept(interceptors ...Interceptor) {
	for _, n := range []interface{ Intercept(...Interceptor) }{
		c.Auction, c.AuctionBid, c.AuthState, c.Card, c.CardStats, c.CardType, c.Cookie,
		c.Daily, c.Dungeon, c.GithubStar, c.Job, c.Listing, c.LootBox, c.Player,
		c.PlayerDeck, c.PlayerFavoriteCards, c.PlayerLimit,
	} {
		n.Intercept(interceptors...)
	}
}

// Mutate implements the ent.Mutator interface.
func (c *Client) Mutate(ctx context.Context, m Mutation) (Value, error) {
	switch m := m.(type) {
	case *AuctionMutation:
		return c.Auction.mutate(ctx, m)
	case *AuctionBidMutation:
		return c.AuctionBid.mutate(ctx, m)
	case *AuthStateMutation:
		return c.AuthState.mutate(ctx, m)
	case *CardMutation:
		return c.Card.mutate(ctx, m)
	case *CardStatsMutation:
		return c.CardStats.mutate(ctx, m)
	case *CardTypeMutation:
		return c.CardType.mutate(ctx, m)
	case *CookieMutation:
		return c.Cookie.mutate(ctx, m)
	case *DailyMutation:
		return c.Daily.mutate(ctx, m)
	case *DungeonMutation:
		return c.Dungeon.mutate(ctx, m)
	case *GithubStarMutation:
		return c.GithubStar.mutate(ctx, m)
	case *JobMutation:
		return c.Job.mutate(ctx, m)
	case *ListingMutation:
		return c.Listing.mutate(ctx, m)
	case *LootBoxMutation:
		return c.LootBox.mutate(ctx, m)
	case *PlayerMutation:
		return c.Player.mutate(ctx, m)
	case *PlayerDeckMutation:
		return c.PlayerDeck.mutate(ctx, m)
	case *PlayerFavoriteCardsMutation:
		return c.PlayerFavoriteCards.mutate(ctx, m)
	case *PlayerLimitMutation:
		return c.PlayerLimit.mutate(ctx, m)
	default:
		return nil, fmt.Errorf("ent: unknown mutation type %T", m)
	}
}

// AuctionClient is a client for the Auction schema.
type AuctionClient struct {
	config
}

// NewAuctionClient returns a client for the Auction from the given config.
func NewAuctionClient(c config) *AuctionClient {
	return &AuctionClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `auction.Hooks(f(g(h())))`.
func (c *AuctionClient) Use(hooks ...Hook) {
	c.hooks.Auction = append(c.hooks.Auction, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `auction.Intercept(f(g(h())))`.
func (c *AuctionClient) Intercept(interceptors ...Interceptor) {
	c.inters.Auction = append(c.inters.Auction, interceptors...)
}

// Create returns a builder for creating a Auction entity.
func (c *AuctionClient) Create() *AuctionCreate {
	mutation := newAuctionMutation(c.config, OpCreate)
	return &AuctionCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Auction entities.
func (c *AuctionClient) CreateBulk(builders ...*AuctionCreate) *AuctionCreateBulk {
	return &AuctionCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *AuctionClient) MapCreateBulk(slice any, setFunc func(*AuctionCreate, int)) *AuctionCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &AuctionCreateBulk{err: fmt.Errorf("calling to AuctionClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*AuctionCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &AuctionCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Auction.
func (c *AuctionClient) Update() *AuctionUpdate {
	mutation := newAuctionMutation(c.config, OpUpdate)
	return &AuctionUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *AuctionClient) UpdateOne(a *Auction) *AuctionUpdateOne {
	mutation := newAuctionMutation(c.config, OpUpdateOne, withAuction(a))
	return &AuctionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *AuctionClient) UpdateOneID(id uuid.UUID) *AuctionUpdateOne {
	mutation := newAuctionMutation(c.config, OpUpdateOne, withAuctionID(id))
	return &AuctionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Auction.
func (c *AuctionClient) Delete() *AuctionDelete {
	mutation := newAuctionMutation(c.config, OpDelete)
	return &AuctionDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *AuctionClient) DeleteOne(a *Auction) *AuctionDeleteOne {
	return c.DeleteOneID(a.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *AuctionClient) DeleteOneID(id uuid.UUID) *AuctionDeleteOne {
	builder := c.Delete().Where(auction.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &AuctionDeleteOne{builder}
}

// Query returns a query builder for Auction.
func (c *AuctionClient) Query() *AuctionQuery {
	return &AuctionQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeAuction},
		inters: c.Interceptors(),
	}
}

// Get returns a Auction entity by its id.
func (c *AuctionClient) Get(ctx context.Context, id uuid.UUID) (*Auction, error) {
	return c.Query().Where(auction.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *AuctionClient) GetX(ctx context.Context, id uuid.UUID) *Auction {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryOwnedBy queries the owned_by edge of a Auction.
func (c *AuctionClient) QueryOwnedBy(a *Auction) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := a.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(auction.Table, auction.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, auction.OwnedByTable, auction.OwnedByColumn),
		)
		fromV = sqlgraph.Neighbors(a.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryCard queries the card edge of a Auction.
func (c *AuctionClient) QueryCard(a *Auction) *CardQuery {
	query := (&CardClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := a.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(auction.Table, auction.FieldID, id),
			sqlgraph.To(card.Table, card.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, auction.CardTable, auction.CardColumn),
		)
		fromV = sqlgraph.Neighbors(a.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryBids queries the bids edge of a Auction.
func (c *AuctionClient) QueryBids(a *Auction) *AuctionBidQuery {
	query := (&AuctionBidClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := a.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(auction.Table, auction.FieldID, id),
			sqlgraph.To(auctionbid.Table, auctionbid.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, auction.BidsTable, auction.BidsColumn),
		)
		fromV = sqlgraph.Neighbors(a.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *AuctionClient) Hooks() []Hook {
	return c.hooks.Auction
}

// Interceptors returns the client interceptors.
func (c *AuctionClient) Interceptors() []Interceptor {
	return c.inters.Auction
}

func (c *AuctionClient) mutate(ctx context.Context, m *AuctionMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&AuctionCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&AuctionUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&AuctionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&AuctionDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Auction mutation op: %q", m.Op())
	}
}

// AuctionBidClient is a client for the AuctionBid schema.
type AuctionBidClient struct {
	config
}

// NewAuctionBidClient returns a client for the AuctionBid from the given config.
func NewAuctionBidClient(c config) *AuctionBidClient {
	return &AuctionBidClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `auctionbid.Hooks(f(g(h())))`.
func (c *AuctionBidClient) Use(hooks ...Hook) {
	c.hooks.AuctionBid = append(c.hooks.AuctionBid, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `auctionbid.Intercept(f(g(h())))`.
func (c *AuctionBidClient) Intercept(interceptors ...Interceptor) {
	c.inters.AuctionBid = append(c.inters.AuctionBid, interceptors...)
}

// Create returns a builder for creating a AuctionBid entity.
func (c *AuctionBidClient) Create() *AuctionBidCreate {
	mutation := newAuctionBidMutation(c.config, OpCreate)
	return &AuctionBidCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of AuctionBid entities.
func (c *AuctionBidClient) CreateBulk(builders ...*AuctionBidCreate) *AuctionBidCreateBulk {
	return &AuctionBidCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *AuctionBidClient) MapCreateBulk(slice any, setFunc func(*AuctionBidCreate, int)) *AuctionBidCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &AuctionBidCreateBulk{err: fmt.Errorf("calling to AuctionBidClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*AuctionBidCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &AuctionBidCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for AuctionBid.
func (c *AuctionBidClient) Update() *AuctionBidUpdate {
	mutation := newAuctionBidMutation(c.config, OpUpdate)
	return &AuctionBidUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *AuctionBidClient) UpdateOne(ab *AuctionBid) *AuctionBidUpdateOne {
	mutation := newAuctionBidMutation(c.config, OpUpdateOne, withAuctionBid(ab))
	return &AuctionBidUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *AuctionBidClient) UpdateOneID(id uuid.UUID) *AuctionBidUpdateOne {
	mutation := newAuctionBidMutation(c.config, OpUpdateOne, withAuctionBidID(id))
	return &AuctionBidUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for AuctionBid.
func (c *AuctionBidClient) Delete() *AuctionBidDelete {
	mutation := newAuctionBidMutation(c.config, OpDelete)
	return &AuctionBidDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *AuctionBidClient) DeleteOne(ab *AuctionBid) *AuctionBidDeleteOne {
	return c.DeleteOneID(ab.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *AuctionBidClient) DeleteOneID(id uuid.UUID) *AuctionBidDeleteOne {
	builder := c.Delete().Where(auctionbid.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &AuctionBidDeleteOne{builder}
}

// Query returns a query builder for AuctionBid.
func (c *AuctionBidClient) Query() *AuctionBidQuery {
	return &AuctionBidQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeAuctionBid},
		inters: c.Interceptors(),
	}
}

// Get returns a AuctionBid entity by its id.
func (c *AuctionBidClient) Get(ctx context.Context, id uuid.UUID) (*AuctionBid, error) {
	return c.Query().Where(auctionbid.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *AuctionBidClient) GetX(ctx context.Context, id uuid.UUID) *AuctionBid {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryAuction queries the auction edge of a AuctionBid.
func (c *AuctionBidClient) QueryAuction(ab *AuctionBid) *AuctionQuery {
	query := (&AuctionClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := ab.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(auctionbid.Table, auctionbid.FieldID, id),
			sqlgraph.To(auction.Table, auction.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, auctionbid.AuctionTable, auctionbid.AuctionColumn),
		)
		fromV = sqlgraph.Neighbors(ab.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryPlayer queries the player edge of a AuctionBid.
func (c *AuctionBidClient) QueryPlayer(ab *AuctionBid) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := ab.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(auctionbid.Table, auctionbid.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, auctionbid.PlayerTable, auctionbid.PlayerColumn),
		)
		fromV = sqlgraph.Neighbors(ab.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *AuctionBidClient) Hooks() []Hook {
	return c.hooks.AuctionBid
}

// Interceptors returns the client interceptors.
func (c *AuctionBidClient) Interceptors() []Interceptor {
	return c.inters.AuctionBid
}

func (c *AuctionBidClient) mutate(ctx context.Context, m *AuctionBidMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&AuctionBidCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&AuctionBidUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&AuctionBidUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&AuctionBidDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown AuctionBid mutation op: %q", m.Op())
	}
}

// AuthStateClient is a client for the AuthState schema.
type AuthStateClient struct {
	config
}

// NewAuthStateClient returns a client for the AuthState from the given config.
func NewAuthStateClient(c config) *AuthStateClient {
	return &AuthStateClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `authstate.Hooks(f(g(h())))`.
func (c *AuthStateClient) Use(hooks ...Hook) {
	c.hooks.AuthState = append(c.hooks.AuthState, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `authstate.Intercept(f(g(h())))`.
func (c *AuthStateClient) Intercept(interceptors ...Interceptor) {
	c.inters.AuthState = append(c.inters.AuthState, interceptors...)
}

// Create returns a builder for creating a AuthState entity.
func (c *AuthStateClient) Create() *AuthStateCreate {
	mutation := newAuthStateMutation(c.config, OpCreate)
	return &AuthStateCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of AuthState entities.
func (c *AuthStateClient) CreateBulk(builders ...*AuthStateCreate) *AuthStateCreateBulk {
	return &AuthStateCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *AuthStateClient) MapCreateBulk(slice any, setFunc func(*AuthStateCreate, int)) *AuthStateCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &AuthStateCreateBulk{err: fmt.Errorf("calling to AuthStateClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*AuthStateCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &AuthStateCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for AuthState.
func (c *AuthStateClient) Update() *AuthStateUpdate {
	mutation := newAuthStateMutation(c.config, OpUpdate)
	return &AuthStateUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *AuthStateClient) UpdateOne(as *AuthState) *AuthStateUpdateOne {
	mutation := newAuthStateMutation(c.config, OpUpdateOne, withAuthState(as))
	return &AuthStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *AuthStateClient) UpdateOneID(id uuid.UUID) *AuthStateUpdateOne {
	mutation := newAuthStateMutation(c.config, OpUpdateOne, withAuthStateID(id))
	return &AuthStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for AuthState.
func (c *AuthStateClient) Delete() *AuthStateDelete {
	mutation := newAuthStateMutation(c.config, OpDelete)
	return &AuthStateDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *AuthStateClient) DeleteOne(as *AuthState) *AuthStateDeleteOne {
	return c.DeleteOneID(as.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *AuthStateClient) DeleteOneID(id uuid.UUID) *AuthStateDeleteOne {
	builder := c.Delete().Where(authstate.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &AuthStateDeleteOne{builder}
}

// Query returns a query builder for AuthState.
func (c *AuthStateClient) Query() *AuthStateQuery {
	return &AuthStateQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeAuthState},
		inters: c.Interceptors(),
	}
}

// Get returns a AuthState entity by its id.
func (c *AuthStateClient) Get(ctx context.Context, id uuid.UUID) (*AuthState, error) {
	return c.Query().Where(authstate.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *AuthStateClient) GetX(ctx context.Context, id uuid.UUID) *AuthState {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryPlayer queries the player edge of a AuthState.
func (c *AuthStateClient) QueryPlayer(as *AuthState) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := as.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(authstate.Table, authstate.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, authstate.PlayerTable, authstate.PlayerColumn),
		)
		fromV = sqlgraph.Neighbors(as.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *AuthStateClient) Hooks() []Hook {
	return c.hooks.AuthState
}

// Interceptors returns the client interceptors.
func (c *AuthStateClient) Interceptors() []Interceptor {
	return c.inters.AuthState
}

func (c *AuthStateClient) mutate(ctx context.Context, m *AuthStateMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&AuthStateCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&AuthStateUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&AuthStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&AuthStateDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown AuthState mutation op: %q", m.Op())
	}
}

// CardClient is a client for the Card schema.
type CardClient struct {
	config
}

// NewCardClient returns a client for the Card from the given config.
func NewCardClient(c config) *CardClient {
	return &CardClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `card.Hooks(f(g(h())))`.
func (c *CardClient) Use(hooks ...Hook) {
	c.hooks.Card = append(c.hooks.Card, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `card.Intercept(f(g(h())))`.
func (c *CardClient) Intercept(interceptors ...Interceptor) {
	c.inters.Card = append(c.inters.Card, interceptors...)
}

// Create returns a builder for creating a Card entity.
func (c *CardClient) Create() *CardCreate {
	mutation := newCardMutation(c.config, OpCreate)
	return &CardCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Card entities.
func (c *CardClient) CreateBulk(builders ...*CardCreate) *CardCreateBulk {
	return &CardCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *CardClient) MapCreateBulk(slice any, setFunc func(*CardCreate, int)) *CardCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &CardCreateBulk{err: fmt.Errorf("calling to CardClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*CardCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &CardCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Card.
func (c *CardClient) Update() *CardUpdate {
	mutation := newCardMutation(c.config, OpUpdate)
	return &CardUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *CardClient) UpdateOne(ca *Card) *CardUpdateOne {
	mutation := newCardMutation(c.config, OpUpdateOne, withCard(ca))
	return &CardUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *CardClient) UpdateOneID(id uuid.UUID) *CardUpdateOne {
	mutation := newCardMutation(c.config, OpUpdateOne, withCardID(id))
	return &CardUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Card.
func (c *CardClient) Delete() *CardDelete {
	mutation := newCardMutation(c.config, OpDelete)
	return &CardDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *CardClient) DeleteOne(ca *Card) *CardDeleteOne {
	return c.DeleteOneID(ca.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *CardClient) DeleteOneID(id uuid.UUID) *CardDeleteOne {
	builder := c.Delete().Where(card.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &CardDeleteOne{builder}
}

// Query returns a query builder for Card.
func (c *CardClient) Query() *CardQuery {
	return &CardQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeCard},
		inters: c.Interceptors(),
	}
}

// Get returns a Card entity by its id.
func (c *CardClient) Get(ctx context.Context, id uuid.UUID) (*Card, error) {
	return c.Query().Where(card.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *CardClient) GetX(ctx context.Context, id uuid.UUID) *Card {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryOwnedBy queries the owned_by edge of a Card.
func (c *CardClient) QueryOwnedBy(ca *Card) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := ca.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(card.Table, card.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, card.OwnedByTable, card.OwnedByColumn),
		)
		fromV = sqlgraph.Neighbors(ca.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryInFavoritesOf queries the in_favorites_of edge of a Card.
func (c *CardClient) QueryInFavoritesOf(ca *Card) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := ca.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(card.Table, card.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, card.InFavoritesOfTable, card.InFavoritesOfPrimaryKey...),
		)
		fromV = sqlgraph.Neighbors(ca.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryInDeckOf queries the in_deck_of edge of a Card.
func (c *CardClient) QueryInDeckOf(ca *Card) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := ca.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(card.Table, card.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, card.InDeckOfTable, card.InDeckOfPrimaryKey...),
		)
		fromV = sqlgraph.Neighbors(ca.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryType queries the type edge of a Card.
func (c *CardClient) QueryType(ca *Card) *CardTypeQuery {
	query := (&CardTypeClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := ca.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(card.Table, card.FieldID, id),
			sqlgraph.To(cardtype.Table, cardtype.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, card.TypeTable, card.TypeColumn),
		)
		fromV = sqlgraph.Neighbors(ca.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryStats queries the stats edge of a Card.
func (c *CardClient) QueryStats(ca *Card) *CardStatsQuery {
	query := (&CardStatsClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := ca.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(card.Table, card.FieldID, id),
			sqlgraph.To(cardstats.Table, cardstats.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, card.StatsTable, card.StatsColumn),
		)
		fromV = sqlgraph.Neighbors(ca.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryPlayerFavoriteCards queries the player_favorite_cards edge of a Card.
func (c *CardClient) QueryPlayerFavoriteCards(ca *Card) *PlayerFavoriteCardsQuery {
	query := (&PlayerFavoriteCardsClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := ca.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(card.Table, card.FieldID, id),
			sqlgraph.To(playerfavoritecards.Table, playerfavoritecards.CardColumn),
			sqlgraph.Edge(sqlgraph.O2M, true, card.PlayerFavoriteCardsTable, card.PlayerFavoriteCardsColumn),
		)
		fromV = sqlgraph.Neighbors(ca.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryPlayerDecks queries the player_decks edge of a Card.
func (c *CardClient) QueryPlayerDecks(ca *Card) *PlayerDeckQuery {
	query := (&PlayerDeckClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := ca.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(card.Table, card.FieldID, id),
			sqlgraph.To(playerdeck.Table, playerdeck.CardColumn),
			sqlgraph.Edge(sqlgraph.O2M, true, card.PlayerDecksTable, card.PlayerDecksColumn),
		)
		fromV = sqlgraph.Neighbors(ca.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *CardClient) Hooks() []Hook {
	return c.hooks.Card
}

// Interceptors returns the client interceptors.
func (c *CardClient) Interceptors() []Interceptor {
	return c.inters.Card
}

func (c *CardClient) mutate(ctx context.Context, m *CardMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&CardCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&CardUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&CardUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&CardDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Card mutation op: %q", m.Op())
	}
}

// CardStatsClient is a client for the CardStats schema.
type CardStatsClient struct {
	config
}

// NewCardStatsClient returns a client for the CardStats from the given config.
func NewCardStatsClient(c config) *CardStatsClient {
	return &CardStatsClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `cardstats.Hooks(f(g(h())))`.
func (c *CardStatsClient) Use(hooks ...Hook) {
	c.hooks.CardStats = append(c.hooks.CardStats, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `cardstats.Intercept(f(g(h())))`.
func (c *CardStatsClient) Intercept(interceptors ...Interceptor) {
	c.inters.CardStats = append(c.inters.CardStats, interceptors...)
}

// Create returns a builder for creating a CardStats entity.
func (c *CardStatsClient) Create() *CardStatsCreate {
	mutation := newCardStatsMutation(c.config, OpCreate)
	return &CardStatsCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of CardStats entities.
func (c *CardStatsClient) CreateBulk(builders ...*CardStatsCreate) *CardStatsCreateBulk {
	return &CardStatsCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *CardStatsClient) MapCreateBulk(slice any, setFunc func(*CardStatsCreate, int)) *CardStatsCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &CardStatsCreateBulk{err: fmt.Errorf("calling to CardStatsClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*CardStatsCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &CardStatsCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for CardStats.
func (c *CardStatsClient) Update() *CardStatsUpdate {
	mutation := newCardStatsMutation(c.config, OpUpdate)
	return &CardStatsUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *CardStatsClient) UpdateOne(cs *CardStats) *CardStatsUpdateOne {
	mutation := newCardStatsMutation(c.config, OpUpdateOne, withCardStats(cs))
	return &CardStatsUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *CardStatsClient) UpdateOneID(id int) *CardStatsUpdateOne {
	mutation := newCardStatsMutation(c.config, OpUpdateOne, withCardStatsID(id))
	return &CardStatsUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for CardStats.
func (c *CardStatsClient) Delete() *CardStatsDelete {
	mutation := newCardStatsMutation(c.config, OpDelete)
	return &CardStatsDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *CardStatsClient) DeleteOne(cs *CardStats) *CardStatsDeleteOne {
	return c.DeleteOneID(cs.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *CardStatsClient) DeleteOneID(id int) *CardStatsDeleteOne {
	builder := c.Delete().Where(cardstats.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &CardStatsDeleteOne{builder}
}

// Query returns a query builder for CardStats.
func (c *CardStatsClient) Query() *CardStatsQuery {
	return &CardStatsQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeCardStats},
		inters: c.Interceptors(),
	}
}

// Get returns a CardStats entity by its id.
func (c *CardStatsClient) Get(ctx context.Context, id int) (*CardStats, error) {
	return c.Query().Where(cardstats.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *CardStatsClient) GetX(ctx context.Context, id int) *CardStats {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryOf queries the of edge of a CardStats.
func (c *CardStatsClient) QueryOf(cs *CardStats) *CardQuery {
	query := (&CardClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := cs.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(cardstats.Table, cardstats.FieldID, id),
			sqlgraph.To(card.Table, card.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, cardstats.OfTable, cardstats.OfColumn),
		)
		fromV = sqlgraph.Neighbors(cs.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *CardStatsClient) Hooks() []Hook {
	return c.hooks.CardStats
}

// Interceptors returns the client interceptors.
func (c *CardStatsClient) Interceptors() []Interceptor {
	return c.inters.CardStats
}

func (c *CardStatsClient) mutate(ctx context.Context, m *CardStatsMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&CardStatsCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&CardStatsUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&CardStatsUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&CardStatsDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown CardStats mutation op: %q", m.Op())
	}
}

// CardTypeClient is a client for the CardType schema.
type CardTypeClient struct {
	config
}

// NewCardTypeClient returns a client for the CardType from the given config.
func NewCardTypeClient(c config) *CardTypeClient {
	return &CardTypeClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `cardtype.Hooks(f(g(h())))`.
func (c *CardTypeClient) Use(hooks ...Hook) {
	c.hooks.CardType = append(c.hooks.CardType, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `cardtype.Intercept(f(g(h())))`.
func (c *CardTypeClient) Intercept(interceptors ...Interceptor) {
	c.inters.CardType = append(c.inters.CardType, interceptors...)
}

// Create returns a builder for creating a CardType entity.
func (c *CardTypeClient) Create() *CardTypeCreate {
	mutation := newCardTypeMutation(c.config, OpCreate)
	return &CardTypeCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of CardType entities.
func (c *CardTypeClient) CreateBulk(builders ...*CardTypeCreate) *CardTypeCreateBulk {
	return &CardTypeCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *CardTypeClient) MapCreateBulk(slice any, setFunc func(*CardTypeCreate, int)) *CardTypeCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &CardTypeCreateBulk{err: fmt.Errorf("calling to CardTypeClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*CardTypeCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &CardTypeCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for CardType.
func (c *CardTypeClient) Update() *CardTypeUpdate {
	mutation := newCardTypeMutation(c.config, OpUpdate)
	return &CardTypeUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *CardTypeClient) UpdateOne(ct *CardType) *CardTypeUpdateOne {
	mutation := newCardTypeMutation(c.config, OpUpdateOne, withCardType(ct))
	return &CardTypeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *CardTypeClient) UpdateOneID(id string) *CardTypeUpdateOne {
	mutation := newCardTypeMutation(c.config, OpUpdateOne, withCardTypeID(id))
	return &CardTypeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for CardType.
func (c *CardTypeClient) Delete() *CardTypeDelete {
	mutation := newCardTypeMutation(c.config, OpDelete)
	return &CardTypeDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *CardTypeClient) DeleteOne(ct *CardType) *CardTypeDeleteOne {
	return c.DeleteOneID(ct.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *CardTypeClient) DeleteOneID(id string) *CardTypeDeleteOne {
	builder := c.Delete().Where(cardtype.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &CardTypeDeleteOne{builder}
}

// Query returns a query builder for CardType.
func (c *CardTypeClient) Query() *CardTypeQuery {
	return &CardTypeQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeCardType},
		inters: c.Interceptors(),
	}
}

// Get returns a CardType entity by its id.
func (c *CardTypeClient) Get(ctx context.Context, id string) (*CardType, error) {
	return c.Query().Where(cardtype.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *CardTypeClient) GetX(ctx context.Context, id string) *CardType {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *CardTypeClient) Hooks() []Hook {
	return c.hooks.CardType
}

// Interceptors returns the client interceptors.
func (c *CardTypeClient) Interceptors() []Interceptor {
	return c.inters.CardType
}

func (c *CardTypeClient) mutate(ctx context.Context, m *CardTypeMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&CardTypeCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&CardTypeUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&CardTypeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&CardTypeDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown CardType mutation op: %q", m.Op())
	}
}

// CookieClient is a client for the Cookie schema.
type CookieClient struct {
	config
}

// NewCookieClient returns a client for the Cookie from the given config.
func NewCookieClient(c config) *CookieClient {
	return &CookieClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `cookie.Hooks(f(g(h())))`.
func (c *CookieClient) Use(hooks ...Hook) {
	c.hooks.Cookie = append(c.hooks.Cookie, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `cookie.Intercept(f(g(h())))`.
func (c *CookieClient) Intercept(interceptors ...Interceptor) {
	c.inters.Cookie = append(c.inters.Cookie, interceptors...)
}

// Create returns a builder for creating a Cookie entity.
func (c *CookieClient) Create() *CookieCreate {
	mutation := newCookieMutation(c.config, OpCreate)
	return &CookieCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Cookie entities.
func (c *CookieClient) CreateBulk(builders ...*CookieCreate) *CookieCreateBulk {
	return &CookieCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *CookieClient) MapCreateBulk(slice any, setFunc func(*CookieCreate, int)) *CookieCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &CookieCreateBulk{err: fmt.Errorf("calling to CookieClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*CookieCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &CookieCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Cookie.
func (c *CookieClient) Update() *CookieUpdate {
	mutation := newCookieMutation(c.config, OpUpdate)
	return &CookieUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *CookieClient) UpdateOne(co *Cookie) *CookieUpdateOne {
	mutation := newCookieMutation(c.config, OpUpdateOne, withCookie(co))
	return &CookieUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *CookieClient) UpdateOneID(id uuid.UUID) *CookieUpdateOne {
	mutation := newCookieMutation(c.config, OpUpdateOne, withCookieID(id))
	return &CookieUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Cookie.
func (c *CookieClient) Delete() *CookieDelete {
	mutation := newCookieMutation(c.config, OpDelete)
	return &CookieDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *CookieClient) DeleteOne(co *Cookie) *CookieDeleteOne {
	return c.DeleteOneID(co.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *CookieClient) DeleteOneID(id uuid.UUID) *CookieDeleteOne {
	builder := c.Delete().Where(cookie.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &CookieDeleteOne{builder}
}

// Query returns a query builder for Cookie.
func (c *CookieClient) Query() *CookieQuery {
	return &CookieQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeCookie},
		inters: c.Interceptors(),
	}
}

// Get returns a Cookie entity by its id.
func (c *CookieClient) Get(ctx context.Context, id uuid.UUID) (*Cookie, error) {
	return c.Query().Where(cookie.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *CookieClient) GetX(ctx context.Context, id uuid.UUID) *Cookie {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryPlayer queries the player edge of a Cookie.
func (c *CookieClient) QueryPlayer(co *Cookie) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := co.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(cookie.Table, cookie.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, cookie.PlayerTable, cookie.PlayerColumn),
		)
		fromV = sqlgraph.Neighbors(co.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *CookieClient) Hooks() []Hook {
	return c.hooks.Cookie
}

// Interceptors returns the client interceptors.
func (c *CookieClient) Interceptors() []Interceptor {
	return c.inters.Cookie
}

func (c *CookieClient) mutate(ctx context.Context, m *CookieMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&CookieCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&CookieUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&CookieUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&CookieDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Cookie mutation op: %q", m.Op())
	}
}

// DailyClient is a client for the Daily schema.
type DailyClient struct {
	config
}

// NewDailyClient returns a client for the Daily from the given config.
func NewDailyClient(c config) *DailyClient {
	return &DailyClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `daily.Hooks(f(g(h())))`.
func (c *DailyClient) Use(hooks ...Hook) {
	c.hooks.Daily = append(c.hooks.Daily, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `daily.Intercept(f(g(h())))`.
func (c *DailyClient) Intercept(interceptors ...Interceptor) {
	c.inters.Daily = append(c.inters.Daily, interceptors...)
}

// Create returns a builder for creating a Daily entity.
func (c *DailyClient) Create() *DailyCreate {
	mutation := newDailyMutation(c.config, OpCreate)
	return &DailyCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Daily entities.
func (c *DailyClient) CreateBulk(builders ...*DailyCreate) *DailyCreateBulk {
	return &DailyCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *DailyClient) MapCreateBulk(slice any, setFunc func(*DailyCreate, int)) *DailyCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &DailyCreateBulk{err: fmt.Errorf("calling to DailyClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*DailyCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &DailyCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Daily.
func (c *DailyClient) Update() *DailyUpdate {
	mutation := newDailyMutation(c.config, OpUpdate)
	return &DailyUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *DailyClient) UpdateOne(d *Daily) *DailyUpdateOne {
	mutation := newDailyMutation(c.config, OpUpdateOne, withDaily(d))
	return &DailyUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *DailyClient) UpdateOneID(id int) *DailyUpdateOne {
	mutation := newDailyMutation(c.config, OpUpdateOne, withDailyID(id))
	return &DailyUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Daily.
func (c *DailyClient) Delete() *DailyDelete {
	mutation := newDailyMutation(c.config, OpDelete)
	return &DailyDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *DailyClient) DeleteOne(d *Daily) *DailyDeleteOne {
	return c.DeleteOneID(d.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *DailyClient) DeleteOneID(id int) *DailyDeleteOne {
	builder := c.Delete().Where(daily.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &DailyDeleteOne{builder}
}

// Query returns a query builder for Daily.
func (c *DailyClient) Query() *DailyQuery {
	return &DailyQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeDaily},
		inters: c.Interceptors(),
	}
}

// Get returns a Daily entity by its id.
func (c *DailyClient) Get(ctx context.Context, id int) (*Daily, error) {
	return c.Query().Where(daily.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *DailyClient) GetX(ctx context.Context, id int) *Daily {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryOf queries the of edge of a Daily.
func (c *DailyClient) QueryOf(d *Daily) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := d.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(daily.Table, daily.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, daily.OfTable, daily.OfColumn),
		)
		fromV = sqlgraph.Neighbors(d.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *DailyClient) Hooks() []Hook {
	return c.hooks.Daily
}

// Interceptors returns the client interceptors.
func (c *DailyClient) Interceptors() []Interceptor {
	return c.inters.Daily
}

func (c *DailyClient) mutate(ctx context.Context, m *DailyMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&DailyCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&DailyUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&DailyUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&DailyDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Daily mutation op: %q", m.Op())
	}
}

// DungeonClient is a client for the Dungeon schema.
type DungeonClient struct {
	config
}

// NewDungeonClient returns a client for the Dungeon from the given config.
func NewDungeonClient(c config) *DungeonClient {
	return &DungeonClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `dungeon.Hooks(f(g(h())))`.
func (c *DungeonClient) Use(hooks ...Hook) {
	c.hooks.Dungeon = append(c.hooks.Dungeon, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `dungeon.Intercept(f(g(h())))`.
func (c *DungeonClient) Intercept(interceptors ...Interceptor) {
	c.inters.Dungeon = append(c.inters.Dungeon, interceptors...)
}

// Create returns a builder for creating a Dungeon entity.
func (c *DungeonClient) Create() *DungeonCreate {
	mutation := newDungeonMutation(c.config, OpCreate)
	return &DungeonCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Dungeon entities.
func (c *DungeonClient) CreateBulk(builders ...*DungeonCreate) *DungeonCreateBulk {
	return &DungeonCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *DungeonClient) MapCreateBulk(slice any, setFunc func(*DungeonCreate, int)) *DungeonCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &DungeonCreateBulk{err: fmt.Errorf("calling to DungeonClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*DungeonCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &DungeonCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Dungeon.
func (c *DungeonClient) Update() *DungeonUpdate {
	mutation := newDungeonMutation(c.config, OpUpdate)
	return &DungeonUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *DungeonClient) UpdateOne(d *Dungeon) *DungeonUpdateOne {
	mutation := newDungeonMutation(c.config, OpUpdateOne, withDungeon(d))
	return &DungeonUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *DungeonClient) UpdateOneID(id uuid.UUID) *DungeonUpdateOne {
	mutation := newDungeonMutation(c.config, OpUpdateOne, withDungeonID(id))
	return &DungeonUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Dungeon.
func (c *DungeonClient) Delete() *DungeonDelete {
	mutation := newDungeonMutation(c.config, OpDelete)
	return &DungeonDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *DungeonClient) DeleteOne(d *Dungeon) *DungeonDeleteOne {
	return c.DeleteOneID(d.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *DungeonClient) DeleteOneID(id uuid.UUID) *DungeonDeleteOne {
	builder := c.Delete().Where(dungeon.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &DungeonDeleteOne{builder}
}

// Query returns a query builder for Dungeon.
func (c *DungeonClient) Query() *DungeonQuery {
	return &DungeonQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeDungeon},
		inters: c.Interceptors(),
	}
}

// Get returns a Dungeon entity by its id.
func (c *DungeonClient) Get(ctx context.Context, id uuid.UUID) (*Dungeon, error) {
	return c.Query().Where(dungeon.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *DungeonClient) GetX(ctx context.Context, id uuid.UUID) *Dungeon {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryOwnedBy queries the owned_by edge of a Dungeon.
func (c *DungeonClient) QueryOwnedBy(d *Dungeon) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := d.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(dungeon.Table, dungeon.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, dungeon.OwnedByTable, dungeon.OwnedByColumn),
		)
		fromV = sqlgraph.Neighbors(d.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *DungeonClient) Hooks() []Hook {
	return c.hooks.Dungeon
}

// Interceptors returns the client interceptors.
func (c *DungeonClient) Interceptors() []Interceptor {
	return c.inters.Dungeon
}

func (c *DungeonClient) mutate(ctx context.Context, m *DungeonMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&DungeonCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&DungeonUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&DungeonUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&DungeonDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Dungeon mutation op: %q", m.Op())
	}
}

// GithubStarClient is a client for the GithubStar schema.
type GithubStarClient struct {
	config
}

// NewGithubStarClient returns a client for the GithubStar from the given config.
func NewGithubStarClient(c config) *GithubStarClient {
	return &GithubStarClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `githubstar.Hooks(f(g(h())))`.
func (c *GithubStarClient) Use(hooks ...Hook) {
	c.hooks.GithubStar = append(c.hooks.GithubStar, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `githubstar.Intercept(f(g(h())))`.
func (c *GithubStarClient) Intercept(interceptors ...Interceptor) {
	c.inters.GithubStar = append(c.inters.GithubStar, interceptors...)
}

// Create returns a builder for creating a GithubStar entity.
func (c *GithubStarClient) Create() *GithubStarCreate {
	mutation := newGithubStarMutation(c.config, OpCreate)
	return &GithubStarCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of GithubStar entities.
func (c *GithubStarClient) CreateBulk(builders ...*GithubStarCreate) *GithubStarCreateBulk {
	return &GithubStarCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *GithubStarClient) MapCreateBulk(slice any, setFunc func(*GithubStarCreate, int)) *GithubStarCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &GithubStarCreateBulk{err: fmt.Errorf("calling to GithubStarClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*GithubStarCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &GithubStarCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for GithubStar.
func (c *GithubStarClient) Update() *GithubStarUpdate {
	mutation := newGithubStarMutation(c.config, OpUpdate)
	return &GithubStarUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *GithubStarClient) UpdateOne(gs *GithubStar) *GithubStarUpdateOne {
	mutation := newGithubStarMutation(c.config, OpUpdateOne, withGithubStar(gs))
	return &GithubStarUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *GithubStarClient) UpdateOneID(id int) *GithubStarUpdateOne {
	mutation := newGithubStarMutation(c.config, OpUpdateOne, withGithubStarID(id))
	return &GithubStarUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for GithubStar.
func (c *GithubStarClient) Delete() *GithubStarDelete {
	mutation := newGithubStarMutation(c.config, OpDelete)
	return &GithubStarDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *GithubStarClient) DeleteOne(gs *GithubStar) *GithubStarDeleteOne {
	return c.DeleteOneID(gs.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *GithubStarClient) DeleteOneID(id int) *GithubStarDeleteOne {
	builder := c.Delete().Where(githubstar.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &GithubStarDeleteOne{builder}
}

// Query returns a query builder for GithubStar.
func (c *GithubStarClient) Query() *GithubStarQuery {
	return &GithubStarQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeGithubStar},
		inters: c.Interceptors(),
	}
}

// Get returns a GithubStar entity by its id.
func (c *GithubStarClient) Get(ctx context.Context, id int) (*GithubStar, error) {
	return c.Query().Where(githubstar.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *GithubStarClient) GetX(ctx context.Context, id int) *GithubStar {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryOf queries the of edge of a GithubStar.
func (c *GithubStarClient) QueryOf(gs *GithubStar) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := gs.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(githubstar.Table, githubstar.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, githubstar.OfTable, githubstar.OfColumn),
		)
		fromV = sqlgraph.Neighbors(gs.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *GithubStarClient) Hooks() []Hook {
	return c.hooks.GithubStar
}

// Interceptors returns the client interceptors.
func (c *GithubStarClient) Interceptors() []Interceptor {
	return c.inters.GithubStar
}

func (c *GithubStarClient) mutate(ctx context.Context, m *GithubStarMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&GithubStarCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&GithubStarUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&GithubStarUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&GithubStarDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown GithubStar mutation op: %q", m.Op())
	}
}

// JobClient is a client for the Job schema.
type JobClient struct {
	config
}

// NewJobClient returns a client for the Job from the given config.
func NewJobClient(c config) *JobClient {
	return &JobClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `job.Hooks(f(g(h())))`.
func (c *JobClient) Use(hooks ...Hook) {
	c.hooks.Job = append(c.hooks.Job, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `job.Intercept(f(g(h())))`.
func (c *JobClient) Intercept(interceptors ...Interceptor) {
	c.inters.Job = append(c.inters.Job, interceptors...)
}

// Create returns a builder for creating a Job entity.
func (c *JobClient) Create() *JobCreate {
	mutation := newJobMutation(c.config, OpCreate)
	return &JobCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Job entities.
func (c *JobClient) CreateBulk(builders ...*JobCreate) *JobCreateBulk {
	return &JobCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *JobClient) MapCreateBulk(slice any, setFunc func(*JobCreate, int)) *JobCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &JobCreateBulk{err: fmt.Errorf("calling to JobClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*JobCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &JobCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Job.
func (c *JobClient) Update() *JobUpdate {
	mutation := newJobMutation(c.config, OpUpdate)
	return &JobUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *JobClient) UpdateOne(j *Job) *JobUpdateOne {
	mutation := newJobMutation(c.config, OpUpdateOne, withJob(j))
	return &JobUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *JobClient) UpdateOneID(id uuid.UUID) *JobUpdateOne {
	mutation := newJobMutation(c.config, OpUpdateOne, withJobID(id))
	return &JobUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Job.
func (c *JobClient) Delete() *JobDelete {
	mutation := newJobMutation(c.config, OpDelete)
	return &JobDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *JobClient) DeleteOne(j *Job) *JobDeleteOne {
	return c.DeleteOneID(j.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *JobClient) DeleteOneID(id uuid.UUID) *JobDeleteOne {
	builder := c.Delete().Where(job.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &JobDeleteOne{builder}
}

// Query returns a query builder for Job.
func (c *JobClient) Query() *JobQuery {
	return &JobQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeJob},
		inters: c.Interceptors(),
	}
}

// Get returns a Job entity by its id.
func (c *JobClient) Get(ctx context.Context, id uuid.UUID) (*Job, error) {
	return c.Query().Where(job.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *JobClient) GetX(ctx context.Context, id uuid.UUID) *Job {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *JobClient) Hooks() []Hook {
	return c.hooks.Job
}

// Interceptors returns the client interceptors.
func (c *JobClient) Interceptors() []Interceptor {
	return c.inters.Job
}

func (c *JobClient) mutate(ctx context.Context, m *JobMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&JobCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&JobUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&JobUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&JobDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Job mutation op: %q", m.Op())
	}
}

// ListingClient is a client for the Listing schema.
type ListingClient struct {
	config
}

// NewListingClient returns a client for the Listing from the given config.
func NewListingClient(c config) *ListingClient {
	return &ListingClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `listing.Hooks(f(g(h())))`.
func (c *ListingClient) Use(hooks ...Hook) {
	c.hooks.Listing = append(c.hooks.Listing, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `listing.Intercept(f(g(h())))`.
func (c *ListingClient) Intercept(interceptors ...Interceptor) {
	c.inters.Listing = append(c.inters.Listing, interceptors...)
}

// Create returns a builder for creating a Listing entity.
func (c *ListingClient) Create() *ListingCreate {
	mutation := newListingMutation(c.config, OpCreate)
	return &ListingCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Listing entities.
func (c *ListingClient) CreateBulk(builders ...*ListingCreate) *ListingCreateBulk {
	return &ListingCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *ListingClient) MapCreateBulk(slice any, setFunc func(*ListingCreate, int)) *ListingCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &ListingCreateBulk{err: fmt.Errorf("calling to ListingClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*ListingCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &ListingCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Listing.
func (c *ListingClient) Update() *ListingUpdate {
	mutation := newListingMutation(c.config, OpUpdate)
	return &ListingUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *ListingClient) UpdateOne(l *Listing) *ListingUpdateOne {
	mutation := newListingMutation(c.config, OpUpdateOne, withListing(l))
	return &ListingUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *ListingClient) UpdateOneID(id uuid.UUID) *ListingUpdateOne {
	mutation := newListingMutation(c.config, OpUpdateOne, withListingID(id))
	return &ListingUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Listing.
func (c *ListingClient) Delete() *ListingDelete {
	mutation := newListingMutation(c.config, OpDelete)
	return &ListingDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *ListingClient) DeleteOne(l *Listing) *ListingDeleteOne {
	return c.DeleteOneID(l.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *ListingClient) DeleteOneID(id uuid.UUID) *ListingDeleteOne {
	builder := c.Delete().Where(listing.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &ListingDeleteOne{builder}
}

// Query returns a query builder for Listing.
func (c *ListingClient) Query() *ListingQuery {
	return &ListingQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeListing},
		inters: c.Interceptors(),
	}
}

// Get returns a Listing entity by its id.
func (c *ListingClient) Get(ctx context.Context, id uuid.UUID) (*Listing, error) {
	return c.Query().Where(listing.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *ListingClient) GetX(ctx context.Context, id uuid.UUID) *Listing {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryOwnedBy queries the owned_by edge of a Listing.
func (c *ListingClient) QueryOwnedBy(l *Listing) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := l.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(listing.Table, listing.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, listing.OwnedByTable, listing.OwnedByColumn),
		)
		fromV = sqlgraph.Neighbors(l.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryCard queries the card edge of a Listing.
func (c *ListingClient) QueryCard(l *Listing) *CardQuery {
	query := (&CardClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := l.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(listing.Table, listing.FieldID, id),
			sqlgraph.To(card.Table, card.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, listing.CardTable, listing.CardColumn),
		)
		fromV = sqlgraph.Neighbors(l.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *ListingClient) Hooks() []Hook {
	return c.hooks.Listing
}

// Interceptors returns the client interceptors.
func (c *ListingClient) Interceptors() []Interceptor {
	return c.inters.Listing
}

func (c *ListingClient) mutate(ctx context.Context, m *ListingMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&ListingCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&ListingUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&ListingUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&ListingDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Listing mutation op: %q", m.Op())
	}
}

// LootBoxClient is a client for the LootBox schema.
type LootBoxClient struct {
	config
}

// NewLootBoxClient returns a client for the LootBox from the given config.
func NewLootBoxClient(c config) *LootBoxClient {
	return &LootBoxClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `lootbox.Hooks(f(g(h())))`.
func (c *LootBoxClient) Use(hooks ...Hook) {
	c.hooks.LootBox = append(c.hooks.LootBox, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `lootbox.Intercept(f(g(h())))`.
func (c *LootBoxClient) Intercept(interceptors ...Interceptor) {
	c.inters.LootBox = append(c.inters.LootBox, interceptors...)
}

// Create returns a builder for creating a LootBox entity.
func (c *LootBoxClient) Create() *LootBoxCreate {
	mutation := newLootBoxMutation(c.config, OpCreate)
	return &LootBoxCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of LootBox entities.
func (c *LootBoxClient) CreateBulk(builders ...*LootBoxCreate) *LootBoxCreateBulk {
	return &LootBoxCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *LootBoxClient) MapCreateBulk(slice any, setFunc func(*LootBoxCreate, int)) *LootBoxCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &LootBoxCreateBulk{err: fmt.Errorf("calling to LootBoxClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*LootBoxCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &LootBoxCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for LootBox.
func (c *LootBoxClient) Update() *LootBoxUpdate {
	mutation := newLootBoxMutation(c.config, OpUpdate)
	return &LootBoxUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *LootBoxClient) UpdateOne(lb *LootBox) *LootBoxUpdateOne {
	mutation := newLootBoxMutation(c.config, OpUpdateOne, withLootBox(lb))
	return &LootBoxUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *LootBoxClient) UpdateOneID(id uuid.UUID) *LootBoxUpdateOne {
	mutation := newLootBoxMutation(c.config, OpUpdateOne, withLootBoxID(id))
	return &LootBoxUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for LootBox.
func (c *LootBoxClient) Delete() *LootBoxDelete {
	mutation := newLootBoxMutation(c.config, OpDelete)
	return &LootBoxDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *LootBoxClient) DeleteOne(lb *LootBox) *LootBoxDeleteOne {
	return c.DeleteOneID(lb.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *LootBoxClient) DeleteOneID(id uuid.UUID) *LootBoxDeleteOne {
	builder := c.Delete().Where(lootbox.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &LootBoxDeleteOne{builder}
}

// Query returns a query builder for LootBox.
func (c *LootBoxClient) Query() *LootBoxQuery {
	return &LootBoxQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeLootBox},
		inters: c.Interceptors(),
	}
}

// Get returns a LootBox entity by its id.
func (c *LootBoxClient) Get(ctx context.Context, id uuid.UUID) (*LootBox, error) {
	return c.Query().Where(lootbox.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *LootBoxClient) GetX(ctx context.Context, id uuid.UUID) *LootBox {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryOwnedBy queries the owned_by edge of a LootBox.
func (c *LootBoxClient) QueryOwnedBy(lb *LootBox) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := lb.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(lootbox.Table, lootbox.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, lootbox.OwnedByTable, lootbox.OwnedByColumn),
		)
		fromV = sqlgraph.Neighbors(lb.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *LootBoxClient) Hooks() []Hook {
	return c.hooks.LootBox
}

// Interceptors returns the client interceptors.
func (c *LootBoxClient) Interceptors() []Interceptor {
	return c.inters.LootBox
}

func (c *LootBoxClient) mutate(ctx context.Context, m *LootBoxMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&LootBoxCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&LootBoxUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&LootBoxUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&LootBoxDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown LootBox mutation op: %q", m.Op())
	}
}

// PlayerClient is a client for the Player schema.
type PlayerClient struct {
	config
}

// NewPlayerClient returns a client for the Player from the given config.
func NewPlayerClient(c config) *PlayerClient {
	return &PlayerClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `player.Hooks(f(g(h())))`.
func (c *PlayerClient) Use(hooks ...Hook) {
	c.hooks.Player = append(c.hooks.Player, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `player.Intercept(f(g(h())))`.
func (c *PlayerClient) Intercept(interceptors ...Interceptor) {
	c.inters.Player = append(c.inters.Player, interceptors...)
}

// Create returns a builder for creating a Player entity.
func (c *PlayerClient) Create() *PlayerCreate {
	mutation := newPlayerMutation(c.config, OpCreate)
	return &PlayerCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Player entities.
func (c *PlayerClient) CreateBulk(builders ...*PlayerCreate) *PlayerCreateBulk {
	return &PlayerCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *PlayerClient) MapCreateBulk(slice any, setFunc func(*PlayerCreate, int)) *PlayerCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &PlayerCreateBulk{err: fmt.Errorf("calling to PlayerClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*PlayerCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &PlayerCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Player.
func (c *PlayerClient) Update() *PlayerUpdate {
	mutation := newPlayerMutation(c.config, OpUpdate)
	return &PlayerUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *PlayerClient) UpdateOne(pl *Player) *PlayerUpdateOne {
	mutation := newPlayerMutation(c.config, OpUpdateOne, withPlayer(pl))
	return &PlayerUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *PlayerClient) UpdateOneID(id string) *PlayerUpdateOne {
	mutation := newPlayerMutation(c.config, OpUpdateOne, withPlayerID(id))
	return &PlayerUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Player.
func (c *PlayerClient) Delete() *PlayerDelete {
	mutation := newPlayerMutation(c.config, OpDelete)
	return &PlayerDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *PlayerClient) DeleteOne(pl *Player) *PlayerDeleteOne {
	return c.DeleteOneID(pl.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *PlayerClient) DeleteOneID(id string) *PlayerDeleteOne {
	builder := c.Delete().Where(player.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &PlayerDeleteOne{builder}
}

// Query returns a query builder for Player.
func (c *PlayerClient) Query() *PlayerQuery {
	return &PlayerQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypePlayer},
		inters: c.Interceptors(),
	}
}

// Get returns a Player entity by its id.
func (c *PlayerClient) Get(ctx context.Context, id string) (*Player, error) {
	return c.Query().Where(player.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *PlayerClient) GetX(ctx context.Context, id string) *Player {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryLimits queries the limits edge of a Player.
func (c *PlayerClient) QueryLimits(pl *Player) *PlayerLimitQuery {
	query := (&PlayerLimitClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(playerlimit.Table, playerlimit.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, player.LimitsTable, player.LimitsColumn),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryCards queries the cards edge of a Player.
func (c *PlayerClient) QueryCards(pl *Player) *CardQuery {
	query := (&CardClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(card.Table, card.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, player.CardsTable, player.CardsColumn),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryFavoriteCards queries the favorite_cards edge of a Player.
func (c *PlayerClient) QueryFavoriteCards(pl *Player) *CardQuery {
	query := (&CardClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(card.Table, card.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, player.FavoriteCardsTable, player.FavoriteCardsPrimaryKey...),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryDeck queries the deck edge of a Player.
func (c *PlayerClient) QueryDeck(pl *Player) *CardQuery {
	query := (&CardClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(card.Table, card.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, player.DeckTable, player.DeckPrimaryKey...),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryLootboxes queries the lootboxes edge of a Player.
func (c *PlayerClient) QueryLootboxes(pl *Player) *LootBoxQuery {
	query := (&LootBoxClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(lootbox.Table, lootbox.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, player.LootboxesTable, player.LootboxesColumn),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QuerySelectedCard queries the selected_card edge of a Player.
func (c *PlayerClient) QuerySelectedCard(pl *Player) *CardQuery {
	query := (&CardClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(card.Table, card.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, player.SelectedCardTable, player.SelectedCardColumn),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryGithubStar queries the github_star edge of a Player.
func (c *PlayerClient) QueryGithubStar(pl *Player) *GithubStarQuery {
	query := (&GithubStarClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(githubstar.Table, githubstar.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, player.GithubStarTable, player.GithubStarColumn),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryDaily queries the daily edge of a Player.
func (c *PlayerClient) QueryDaily(pl *Player) *DailyQuery {
	query := (&DailyClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(daily.Table, daily.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, player.DailyTable, player.DailyColumn),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryAuctions queries the auctions edge of a Player.
func (c *PlayerClient) QueryAuctions(pl *Player) *AuctionQuery {
	query := (&AuctionClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(auction.Table, auction.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, player.AuctionsTable, player.AuctionsColumn),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryListings queries the listings edge of a Player.
func (c *PlayerClient) QueryListings(pl *Player) *ListingQuery {
	query := (&ListingClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(listing.Table, listing.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, player.ListingsTable, player.ListingsColumn),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryDungeons queries the dungeons edge of a Player.
func (c *PlayerClient) QueryDungeons(pl *Player) *DungeonQuery {
	query := (&DungeonClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(dungeon.Table, dungeon.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, player.DungeonsTable, player.DungeonsColumn),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryPlayerFavoriteCards queries the player_favorite_cards edge of a Player.
func (c *PlayerClient) QueryPlayerFavoriteCards(pl *Player) *PlayerFavoriteCardsQuery {
	query := (&PlayerFavoriteCardsClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(playerfavoritecards.Table, playerfavoritecards.PlayerColumn),
			sqlgraph.Edge(sqlgraph.O2M, true, player.PlayerFavoriteCardsTable, player.PlayerFavoriteCardsColumn),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// QueryPlayerDecks queries the player_decks edge of a Player.
func (c *PlayerClient) QueryPlayerDecks(pl *Player) *PlayerDeckQuery {
	query := (&PlayerDeckClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(player.Table, player.FieldID, id),
			sqlgraph.To(playerdeck.Table, playerdeck.PlayerColumn),
			sqlgraph.Edge(sqlgraph.O2M, true, player.PlayerDecksTable, player.PlayerDecksColumn),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *PlayerClient) Hooks() []Hook {
	return c.hooks.Player
}

// Interceptors returns the client interceptors.
func (c *PlayerClient) Interceptors() []Interceptor {
	return c.inters.Player
}

func (c *PlayerClient) mutate(ctx context.Context, m *PlayerMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&PlayerCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&PlayerUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&PlayerUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&PlayerDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Player mutation op: %q", m.Op())
	}
}

// PlayerDeckClient is a client for the PlayerDeck schema.
type PlayerDeckClient struct {
	config
}

// NewPlayerDeckClient returns a client for the PlayerDeck from the given config.
func NewPlayerDeckClient(c config) *PlayerDeckClient {
	return &PlayerDeckClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `playerdeck.Hooks(f(g(h())))`.
func (c *PlayerDeckClient) Use(hooks ...Hook) {
	c.hooks.PlayerDeck = append(c.hooks.PlayerDeck, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `playerdeck.Intercept(f(g(h())))`.
func (c *PlayerDeckClient) Intercept(interceptors ...Interceptor) {
	c.inters.PlayerDeck = append(c.inters.PlayerDeck, interceptors...)
}

// Create returns a builder for creating a PlayerDeck entity.
func (c *PlayerDeckClient) Create() *PlayerDeckCreate {
	mutation := newPlayerDeckMutation(c.config, OpCreate)
	return &PlayerDeckCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of PlayerDeck entities.
func (c *PlayerDeckClient) CreateBulk(builders ...*PlayerDeckCreate) *PlayerDeckCreateBulk {
	return &PlayerDeckCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *PlayerDeckClient) MapCreateBulk(slice any, setFunc func(*PlayerDeckCreate, int)) *PlayerDeckCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &PlayerDeckCreateBulk{err: fmt.Errorf("calling to PlayerDeckClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*PlayerDeckCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &PlayerDeckCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for PlayerDeck.
func (c *PlayerDeckClient) Update() *PlayerDeckUpdate {
	mutation := newPlayerDeckMutation(c.config, OpUpdate)
	return &PlayerDeckUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *PlayerDeckClient) UpdateOne(pd *PlayerDeck) *PlayerDeckUpdateOne {
	mutation := newPlayerDeckMutation(c.config, OpUpdateOne)
	mutation.player = &pd.PlayerID
	mutation.card = &pd.CardID
	return &PlayerDeckUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for PlayerDeck.
func (c *PlayerDeckClient) Delete() *PlayerDeckDelete {
	mutation := newPlayerDeckMutation(c.config, OpDelete)
	return &PlayerDeckDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Query returns a query builder for PlayerDeck.
func (c *PlayerDeckClient) Query() *PlayerDeckQuery {
	return &PlayerDeckQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypePlayerDeck},
		inters: c.Interceptors(),
	}
}

// QueryPlayer queries the player edge of a PlayerDeck.
func (c *PlayerDeckClient) QueryPlayer(pd *PlayerDeck) *PlayerQuery {
	return c.Query().
		Where(playerdeck.PlayerID(pd.PlayerID), playerdeck.CardID(pd.CardID)).
		QueryPlayer()
}

// QueryCard queries the card edge of a PlayerDeck.
func (c *PlayerDeckClient) QueryCard(pd *PlayerDeck) *CardQuery {
	return c.Query().
		Where(playerdeck.PlayerID(pd.PlayerID), playerdeck.CardID(pd.CardID)).
		QueryCard()
}

// Hooks returns the client hooks.
func (c *PlayerDeckClient) Hooks() []Hook {
	return c.hooks.PlayerDeck
}

// Interceptors returns the client interceptors.
func (c *PlayerDeckClient) Interceptors() []Interceptor {
	return c.inters.PlayerDeck
}

func (c *PlayerDeckClient) mutate(ctx context.Context, m *PlayerDeckMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&PlayerDeckCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&PlayerDeckUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&PlayerDeckUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&PlayerDeckDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown PlayerDeck mutation op: %q", m.Op())
	}
}

// PlayerFavoriteCardsClient is a client for the PlayerFavoriteCards schema.
type PlayerFavoriteCardsClient struct {
	config
}

// NewPlayerFavoriteCardsClient returns a client for the PlayerFavoriteCards from the given config.
func NewPlayerFavoriteCardsClient(c config) *PlayerFavoriteCardsClient {
	return &PlayerFavoriteCardsClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `playerfavoritecards.Hooks(f(g(h())))`.
func (c *PlayerFavoriteCardsClient) Use(hooks ...Hook) {
	c.hooks.PlayerFavoriteCards = append(c.hooks.PlayerFavoriteCards, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `playerfavoritecards.Intercept(f(g(h())))`.
func (c *PlayerFavoriteCardsClient) Intercept(interceptors ...Interceptor) {
	c.inters.PlayerFavoriteCards = append(c.inters.PlayerFavoriteCards, interceptors...)
}

// Create returns a builder for creating a PlayerFavoriteCards entity.
func (c *PlayerFavoriteCardsClient) Create() *PlayerFavoriteCardsCreate {
	mutation := newPlayerFavoriteCardsMutation(c.config, OpCreate)
	return &PlayerFavoriteCardsCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of PlayerFavoriteCards entities.
func (c *PlayerFavoriteCardsClient) CreateBulk(builders ...*PlayerFavoriteCardsCreate) *PlayerFavoriteCardsCreateBulk {
	return &PlayerFavoriteCardsCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *PlayerFavoriteCardsClient) MapCreateBulk(slice any, setFunc func(*PlayerFavoriteCardsCreate, int)) *PlayerFavoriteCardsCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &PlayerFavoriteCardsCreateBulk{err: fmt.Errorf("calling to PlayerFavoriteCardsClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*PlayerFavoriteCardsCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &PlayerFavoriteCardsCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for PlayerFavoriteCards.
func (c *PlayerFavoriteCardsClient) Update() *PlayerFavoriteCardsUpdate {
	mutation := newPlayerFavoriteCardsMutation(c.config, OpUpdate)
	return &PlayerFavoriteCardsUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *PlayerFavoriteCardsClient) UpdateOne(pfc *PlayerFavoriteCards) *PlayerFavoriteCardsUpdateOne {
	mutation := newPlayerFavoriteCardsMutation(c.config, OpUpdateOne)
	mutation.player = &pfc.PlayerID
	mutation.card = &pfc.CardID
	return &PlayerFavoriteCardsUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for PlayerFavoriteCards.
func (c *PlayerFavoriteCardsClient) Delete() *PlayerFavoriteCardsDelete {
	mutation := newPlayerFavoriteCardsMutation(c.config, OpDelete)
	return &PlayerFavoriteCardsDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Query returns a query builder for PlayerFavoriteCards.
func (c *PlayerFavoriteCardsClient) Query() *PlayerFavoriteCardsQuery {
	return &PlayerFavoriteCardsQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypePlayerFavoriteCards},
		inters: c.Interceptors(),
	}
}

// QueryPlayer queries the player edge of a PlayerFavoriteCards.
func (c *PlayerFavoriteCardsClient) QueryPlayer(pfc *PlayerFavoriteCards) *PlayerQuery {
	return c.Query().
		Where(playerfavoritecards.PlayerID(pfc.PlayerID), playerfavoritecards.CardID(pfc.CardID)).
		QueryPlayer()
}

// QueryCard queries the card edge of a PlayerFavoriteCards.
func (c *PlayerFavoriteCardsClient) QueryCard(pfc *PlayerFavoriteCards) *CardQuery {
	return c.Query().
		Where(playerfavoritecards.PlayerID(pfc.PlayerID), playerfavoritecards.CardID(pfc.CardID)).
		QueryCard()
}

// Hooks returns the client hooks.
func (c *PlayerFavoriteCardsClient) Hooks() []Hook {
	return c.hooks.PlayerFavoriteCards
}

// Interceptors returns the client interceptors.
func (c *PlayerFavoriteCardsClient) Interceptors() []Interceptor {
	return c.inters.PlayerFavoriteCards
}

func (c *PlayerFavoriteCardsClient) mutate(ctx context.Context, m *PlayerFavoriteCardsMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&PlayerFavoriteCardsCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&PlayerFavoriteCardsUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&PlayerFavoriteCardsUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&PlayerFavoriteCardsDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown PlayerFavoriteCards mutation op: %q", m.Op())
	}
}

// PlayerLimitClient is a client for the PlayerLimit schema.
type PlayerLimitClient struct {
	config
}

// NewPlayerLimitClient returns a client for the PlayerLimit from the given config.
func NewPlayerLimitClient(c config) *PlayerLimitClient {
	return &PlayerLimitClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `playerlimit.Hooks(f(g(h())))`.
func (c *PlayerLimitClient) Use(hooks ...Hook) {
	c.hooks.PlayerLimit = append(c.hooks.PlayerLimit, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `playerlimit.Intercept(f(g(h())))`.
func (c *PlayerLimitClient) Intercept(interceptors ...Interceptor) {
	c.inters.PlayerLimit = append(c.inters.PlayerLimit, interceptors...)
}

// Create returns a builder for creating a PlayerLimit entity.
func (c *PlayerLimitClient) Create() *PlayerLimitCreate {
	mutation := newPlayerLimitMutation(c.config, OpCreate)
	return &PlayerLimitCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of PlayerLimit entities.
func (c *PlayerLimitClient) CreateBulk(builders ...*PlayerLimitCreate) *PlayerLimitCreateBulk {
	return &PlayerLimitCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *PlayerLimitClient) MapCreateBulk(slice any, setFunc func(*PlayerLimitCreate, int)) *PlayerLimitCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &PlayerLimitCreateBulk{err: fmt.Errorf("calling to PlayerLimitClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*PlayerLimitCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &PlayerLimitCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for PlayerLimit.
func (c *PlayerLimitClient) Update() *PlayerLimitUpdate {
	mutation := newPlayerLimitMutation(c.config, OpUpdate)
	return &PlayerLimitUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *PlayerLimitClient) UpdateOne(pl *PlayerLimit) *PlayerLimitUpdateOne {
	mutation := newPlayerLimitMutation(c.config, OpUpdateOne, withPlayerLimit(pl))
	return &PlayerLimitUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *PlayerLimitClient) UpdateOneID(id int) *PlayerLimitUpdateOne {
	mutation := newPlayerLimitMutation(c.config, OpUpdateOne, withPlayerLimitID(id))
	return &PlayerLimitUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for PlayerLimit.
func (c *PlayerLimitClient) Delete() *PlayerLimitDelete {
	mutation := newPlayerLimitMutation(c.config, OpDelete)
	return &PlayerLimitDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *PlayerLimitClient) DeleteOne(pl *PlayerLimit) *PlayerLimitDeleteOne {
	return c.DeleteOneID(pl.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *PlayerLimitClient) DeleteOneID(id int) *PlayerLimitDeleteOne {
	builder := c.Delete().Where(playerlimit.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &PlayerLimitDeleteOne{builder}
}

// Query returns a query builder for PlayerLimit.
func (c *PlayerLimitClient) Query() *PlayerLimitQuery {
	return &PlayerLimitQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypePlayerLimit},
		inters: c.Interceptors(),
	}
}

// Get returns a PlayerLimit entity by its id.
func (c *PlayerLimitClient) Get(ctx context.Context, id int) (*PlayerLimit, error) {
	return c.Query().Where(playerlimit.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *PlayerLimitClient) GetX(ctx context.Context, id int) *PlayerLimit {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryOf queries the of edge of a PlayerLimit.
func (c *PlayerLimitClient) QueryOf(pl *PlayerLimit) *PlayerQuery {
	query := (&PlayerClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := pl.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(playerlimit.Table, playerlimit.FieldID, id),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, playerlimit.OfTable, playerlimit.OfColumn),
		)
		fromV = sqlgraph.Neighbors(pl.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *PlayerLimitClient) Hooks() []Hook {
	return c.hooks.PlayerLimit
}

// Interceptors returns the client interceptors.
func (c *PlayerLimitClient) Interceptors() []Interceptor {
	return c.inters.PlayerLimit
}

func (c *PlayerLimitClient) mutate(ctx context.Context, m *PlayerLimitMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&PlayerLimitCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&PlayerLimitUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&PlayerLimitUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&PlayerLimitDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown PlayerLimit mutation op: %q", m.Op())
	}
}

// hooks and interceptors per client, for fast access.
type (
	hooks struct {
		Auction, AuctionBid, AuthState, Card, CardStats, CardType, Cookie, Daily,
		Dungeon, GithubStar, Job, Listing, LootBox, Player, PlayerDeck,
		PlayerFavoriteCards, PlayerLimit []ent.Hook
	}
	inters struct {
		Auction, AuctionBid, AuthState, Card, CardStats, CardType, Cookie, Daily,
		Dungeon, GithubStar, Job, Listing, LootBox, Player, PlayerDeck,
		PlayerFavoriteCards, PlayerLimit []ent.Interceptor
	}
)
