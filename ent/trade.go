// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yyewolf/rwbyadv3/ent/player"
	"github.com/yyewolf/rwbyadv3/ent/trade"
)

// Trade is the model entity for the Trade schema.
type Trade struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty,omitzero"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty,omitzero"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty,omitzero"`
	// InitiatorID holds the value of the "initiator_id" field.
	InitiatorID string `json:"initiator_id,omitempty,omitzero"`
	// ReceiverID holds the value of the "receiver_id" field.
	ReceiverID string `json:"receiver_id,omitempty,omitzero"`
	// Liens holds the value of the "liens" field.
	Liens int `json:"liens,omitempty,omitzero"`
	// OfferCards holds the value of the "offer_cards" field.
	OfferCards []string `json:"offer_cards,omitempty,omitzero"`
	// ReceiveCards holds the value of the "receive_cards" field.
	ReceiveCards []string `json:"receive_cards,omitempty,omitzero"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TradeQuery when eager-loading is set.
	Edges        TradeEdges `json:"edges,omitempty,omitzero"`
	selectValues sql.SelectValues
}

// TradeEdges holds the relations/edges for other nodes in the graph.
type TradeEdges struct {
	// Initiator holds the value of the initiator edge.
	Initiator *Player `json:"initiator,omitempty,omitzero"`
	// Receiver holds the value of the receiver edge.
	Receiver *Player `json:"receiver,omitempty,omitzero"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// InitiatorOrErr returns the Initiator value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TradeEdges) InitiatorOrErr() (*Player, error) {
	if e.Initiator != nil {
		return e.Initiator, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: player.Label}
	}
	return nil, &NotLoadedError{edge: "initiator"}
}

// ReceiverOrErr returns the Receiver value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TradeEdges) ReceiverOrErr() (*Player, error) {
	if e.Receiver != nil {
		return e.Receiver, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: player.Label}
	}
	return nil, &NotLoadedError{edge: "receiver"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Trade) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case trade.FieldOfferCards, trade.FieldReceiveCards:
			values[i] = new([]byte)
		case trade.FieldLiens:
			values[i] = new(sql.NullInt64)
		case trade.FieldInitiatorID, trade.FieldReceiverID:
			values[i] = new(sql.NullString)
		case trade.FieldCreateTime, trade.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case trade.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Trade fields.
func (t *Trade) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case trade.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				t.ID = *value
			}
		case trade.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				t.CreateTime = value.Time
			}
		case trade.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				t.UpdateTime = value.Time
			}
		case trade.FieldInitiatorID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field initiator_id", values[i])
			} else if value.Valid {
				t.InitiatorID = value.String
			}
		case trade.FieldReceiverID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field receiver_id", values[i])
			} else if value.Valid {
				t.ReceiverID = value.String
			}
		case trade.FieldLiens:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field liens", values[i])
			} else if value.Valid {
				t.Liens = int(value.Int64)
			}
		case trade.FieldOfferCards:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field offer_cards", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &t.OfferCards); err != nil {
					return fmt.Errorf("unmarshal field offer_cards: %w", err)
				}
			}
		case trade.FieldReceiveCards:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field receive_cards", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &t.ReceiveCards); err != nil {
					return fmt.Errorf("unmarshal field receive_cards: %w", err)
				}
			}
		default:
			t.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Trade.
// This includes values selected through modifiers, order, etc.
func (t *Trade) Value(name string) (ent.Value, error) {
	return t.selectValues.Get(name)
}

// QueryInitiator queries the "initiator" edge of the Trade entity.
func (t *Trade) QueryInitiator() *PlayerQuery {
	return NewTradeClient(t.config).QueryInitiator(t)
}

// QueryReceiver queries the "receiver" edge of the Trade entity.
func (t *Trade) QueryReceiver() *PlayerQuery {
	return NewTradeClient(t.config).QueryReceiver(t)
}

// Update returns a builder for updating this Trade.
// Note that you need to call Trade.Unwrap() before calling this method if this Trade
// was returned from a transaction, and the transaction was committed or rolled back.
func (t *Trade) Update() *TradeUpdateOne {
	return NewTradeClient(t.config).UpdateOne(t)
}

// Unwrap unwraps the Trade entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (t *Trade) Unwrap() *Trade {
	_tx, ok := t.config.driver.(*txDriver)
	if !ok {
		panic("ent: Trade is not a transactional entity")
	}
	t.config.driver = _tx.drv
	return t
}

// String implements the fmt.Stringer.
func (t *Trade) String() string {
	var builder strings.Builder
	builder.WriteString("Trade(")
	builder.WriteString(fmt.Sprintf("id=%v, ", t.ID))
	builder.WriteString("create_time=")
	builder.WriteString(t.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(t.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("initiator_id=")
	builder.WriteString(t.InitiatorID)
	builder.WriteString(", ")
	builder.WriteString("receiver_id=")
	builder.WriteString(t.ReceiverID)
	builder.WriteString(", ")
	builder.WriteString("liens=")
	builder.WriteString(fmt.Sprintf("%v", t.Liens))
	builder.WriteString(", ")
	builder.WriteString("offer_cards=")
	builder.WriteString(fmt.Sprintf("%v", t.OfferCards))
	builder.WriteString(", ")
	builder.WriteString("receive_cards=")
	builder.WriteString(fmt.Sprintf("%v", t.ReceiveCards))
	builder.WriteByte(')')
	return builder.String()
}

// Trades is a parsable slice of Trade.
type Trades []*Trade
