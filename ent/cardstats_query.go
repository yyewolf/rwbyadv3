// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/yyewolf/rwbyadv3/ent/card"
	"github.com/yyewolf/rwbyadv3/ent/cardstats"
	"github.com/yyewolf/rwbyadv3/ent/predicate"
)

// CardStatsQuery is the builder for querying CardStats entities.
type CardStatsQuery struct {
	config
	ctx        *QueryContext
	order      []cardstats.OrderOption
	inters     []Interceptor
	predicates []predicate.CardStats
	withOf     *CardQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CardStatsQuery builder.
func (csq *CardStatsQuery) Where(ps ...predicate.CardStats) *CardStatsQuery {
	csq.predicates = append(csq.predicates, ps...)
	return csq
}

// Limit the number of records to be returned by this query.
func (csq *CardStatsQuery) Limit(limit int) *CardStatsQuery {
	csq.ctx.Limit = &limit
	return csq
}

// Offset to start from.
func (csq *CardStatsQuery) Offset(offset int) *CardStatsQuery {
	csq.ctx.Offset = &offset
	return csq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (csq *CardStatsQuery) Unique(unique bool) *CardStatsQuery {
	csq.ctx.Unique = &unique
	return csq
}

// Order specifies how the records should be ordered.
func (csq *CardStatsQuery) Order(o ...cardstats.OrderOption) *CardStatsQuery {
	csq.order = append(csq.order, o...)
	return csq
}

// QueryOf chains the current query on the "of" edge.
func (csq *CardStatsQuery) QueryOf() *CardQuery {
	query := (&CardClient{config: csq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cardstats.Table, cardstats.FieldID, selector),
			sqlgraph.To(card.Table, card.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, cardstats.OfTable, cardstats.OfColumn),
		)
		fromU = sqlgraph.SetNeighbors(csq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CardStats entity from the query.
// Returns a *NotFoundError when no CardStats was found.
func (csq *CardStatsQuery) First(ctx context.Context) (*CardStats, error) {
	nodes, err := csq.Limit(1).All(setContextOp(ctx, csq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{cardstats.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (csq *CardStatsQuery) FirstX(ctx context.Context) *CardStats {
	node, err := csq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CardStats ID from the query.
// Returns a *NotFoundError when no CardStats ID was found.
func (csq *CardStatsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = csq.Limit(1).IDs(setContextOp(ctx, csq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{cardstats.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (csq *CardStatsQuery) FirstIDX(ctx context.Context) int {
	id, err := csq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CardStats entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CardStats entity is found.
// Returns a *NotFoundError when no CardStats entities are found.
func (csq *CardStatsQuery) Only(ctx context.Context) (*CardStats, error) {
	nodes, err := csq.Limit(2).All(setContextOp(ctx, csq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{cardstats.Label}
	default:
		return nil, &NotSingularError{cardstats.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (csq *CardStatsQuery) OnlyX(ctx context.Context) *CardStats {
	node, err := csq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CardStats ID in the query.
// Returns a *NotSingularError when more than one CardStats ID is found.
// Returns a *NotFoundError when no entities are found.
func (csq *CardStatsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = csq.Limit(2).IDs(setContextOp(ctx, csq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{cardstats.Label}
	default:
		err = &NotSingularError{cardstats.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (csq *CardStatsQuery) OnlyIDX(ctx context.Context) int {
	id, err := csq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CardStatsSlice.
func (csq *CardStatsQuery) All(ctx context.Context) ([]*CardStats, error) {
	ctx = setContextOp(ctx, csq.ctx, ent.OpQueryAll)
	if err := csq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CardStats, *CardStatsQuery]()
	return withInterceptors[[]*CardStats](ctx, csq, qr, csq.inters)
}

// AllX is like All, but panics if an error occurs.
func (csq *CardStatsQuery) AllX(ctx context.Context) []*CardStats {
	nodes, err := csq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CardStats IDs.
func (csq *CardStatsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if csq.ctx.Unique == nil && csq.path != nil {
		csq.Unique(true)
	}
	ctx = setContextOp(ctx, csq.ctx, ent.OpQueryIDs)
	if err = csq.Select(cardstats.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (csq *CardStatsQuery) IDsX(ctx context.Context) []int {
	ids, err := csq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (csq *CardStatsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, csq.ctx, ent.OpQueryCount)
	if err := csq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, csq, querierCount[*CardStatsQuery](), csq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (csq *CardStatsQuery) CountX(ctx context.Context) int {
	count, err := csq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (csq *CardStatsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, csq.ctx, ent.OpQueryExist)
	switch _, err := csq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (csq *CardStatsQuery) ExistX(ctx context.Context) bool {
	exist, err := csq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CardStatsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (csq *CardStatsQuery) Clone() *CardStatsQuery {
	if csq == nil {
		return nil
	}
	return &CardStatsQuery{
		config:     csq.config,
		ctx:        csq.ctx.Clone(),
		order:      append([]cardstats.OrderOption{}, csq.order...),
		inters:     append([]Interceptor{}, csq.inters...),
		predicates: append([]predicate.CardStats{}, csq.predicates...),
		withOf:     csq.withOf.Clone(),
		// clone intermediate query.
		sql:  csq.sql.Clone(),
		path: csq.path,
	}
}

// WithOf tells the query-builder to eager-load the nodes that are connected to
// the "of" edge. The optional arguments are used to configure the query builder of the edge.
func (csq *CardStatsQuery) WithOf(opts ...func(*CardQuery)) *CardStatsQuery {
	query := (&CardClient{config: csq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csq.withOf = query
	return csq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty,omitzero"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CardStats.Query().
//		GroupBy(cardstats.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (csq *CardStatsQuery) GroupBy(field string, fields ...string) *CardStatsGroupBy {
	csq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CardStatsGroupBy{build: csq}
	grbuild.flds = &csq.ctx.Fields
	grbuild.label = cardstats.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty,omitzero"`
//	}
//
//	client.CardStats.Query().
//		Select(cardstats.FieldCreateTime).
//		Scan(ctx, &v)
func (csq *CardStatsQuery) Select(fields ...string) *CardStatsSelect {
	csq.ctx.Fields = append(csq.ctx.Fields, fields...)
	sbuild := &CardStatsSelect{CardStatsQuery: csq}
	sbuild.label = cardstats.Label
	sbuild.flds, sbuild.scan = &csq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CardStatsSelect configured with the given aggregations.
func (csq *CardStatsQuery) Aggregate(fns ...AggregateFunc) *CardStatsSelect {
	return csq.Select().Aggregate(fns...)
}

func (csq *CardStatsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range csq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, csq); err != nil {
				return err
			}
		}
	}
	for _, f := range csq.ctx.Fields {
		if !cardstats.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if csq.path != nil {
		prev, err := csq.path(ctx)
		if err != nil {
			return err
		}
		csq.sql = prev
	}
	return nil
}

func (csq *CardStatsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CardStats, error) {
	var (
		nodes       = []*CardStats{}
		_spec       = csq.querySpec()
		loadedTypes = [1]bool{
			csq.withOf != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CardStats).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CardStats{config: csq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, csq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := csq.withOf; query != nil {
		if err := csq.loadOf(ctx, query, nodes, nil,
			func(n *CardStats, e *Card) { n.Edges.Of = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (csq *CardStatsQuery) loadOf(ctx context.Context, query *CardQuery, nodes []*CardStats, init func(*CardStats), assign func(*CardStats, *Card)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*CardStats)
	for i := range nodes {
		fk := nodes[i].CardID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(card.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "card_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (csq *CardStatsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := csq.querySpec()
	_spec.Node.Columns = csq.ctx.Fields
	if len(csq.ctx.Fields) > 0 {
		_spec.Unique = csq.ctx.Unique != nil && *csq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, csq.driver, _spec)
}

func (csq *CardStatsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(cardstats.Table, cardstats.Columns, sqlgraph.NewFieldSpec(cardstats.FieldID, field.TypeInt))
	_spec.From = csq.sql
	if unique := csq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if csq.path != nil {
		_spec.Unique = true
	}
	if fields := csq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, cardstats.FieldID)
		for i := range fields {
			if fields[i] != cardstats.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if csq.withOf != nil {
			_spec.Node.AddColumnOnce(cardstats.FieldCardID)
		}
	}
	if ps := csq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := csq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := csq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := csq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (csq *CardStatsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(csq.driver.Dialect())
	t1 := builder.Table(cardstats.Table)
	columns := csq.ctx.Fields
	if len(columns) == 0 {
		columns = cardstats.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if csq.sql != nil {
		selector = csq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if csq.ctx.Unique != nil && *csq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range csq.predicates {
		p(selector)
	}
	for _, p := range csq.order {
		p(selector)
	}
	if offset := csq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := csq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CardStatsGroupBy is the group-by builder for CardStats entities.
type CardStatsGroupBy struct {
	selector
	build *CardStatsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (csgb *CardStatsGroupBy) Aggregate(fns ...AggregateFunc) *CardStatsGroupBy {
	csgb.fns = append(csgb.fns, fns...)
	return csgb
}

// Scan applies the selector query and scans the result into the given value.
func (csgb *CardStatsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, csgb.build.ctx, ent.OpQueryGroupBy)
	if err := csgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CardStatsQuery, *CardStatsGroupBy](ctx, csgb.build, csgb, csgb.build.inters, v)
}

func (csgb *CardStatsGroupBy) sqlScan(ctx context.Context, root *CardStatsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(csgb.fns))
	for _, fn := range csgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*csgb.flds)+len(csgb.fns))
		for _, f := range *csgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*csgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := csgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CardStatsSelect is the builder for selecting fields of CardStats entities.
type CardStatsSelect struct {
	*CardStatsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (css *CardStatsSelect) Aggregate(fns ...AggregateFunc) *CardStatsSelect {
	css.fns = append(css.fns, fns...)
	return css
}

// Scan applies the selector query and scans the result into the given value.
func (css *CardStatsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, css.ctx, ent.OpQuerySelect)
	if err := css.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CardStatsQuery, *CardStatsSelect](ctx, css.CardStatsQuery, css, css.inters, v)
}

func (css *CardStatsSelect) sqlScan(ctx context.Context, root *CardStatsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(css.fns))
	for _, fn := range css.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*css.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := css.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
