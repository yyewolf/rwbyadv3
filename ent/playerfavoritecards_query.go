// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"github.com/google/uuid"
	"github.com/yyewolf/rwbyadv3/ent/card"
	"github.com/yyewolf/rwbyadv3/ent/player"
	"github.com/yyewolf/rwbyadv3/ent/playerfavoritecards"
	"github.com/yyewolf/rwbyadv3/ent/predicate"
)

// PlayerFavoriteCardsQuery is the builder for querying PlayerFavoriteCards entities.
type PlayerFavoriteCardsQuery struct {
	config
	ctx        *QueryContext
	order      []playerfavoritecards.OrderOption
	inters     []Interceptor
	predicates []predicate.PlayerFavoriteCards
	withPlayer *PlayerQuery
	withCard   *CardQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PlayerFavoriteCardsQuery builder.
func (pfcq *PlayerFavoriteCardsQuery) Where(ps ...predicate.PlayerFavoriteCards) *PlayerFavoriteCardsQuery {
	pfcq.predicates = append(pfcq.predicates, ps...)
	return pfcq
}

// Limit the number of records to be returned by this query.
func (pfcq *PlayerFavoriteCardsQuery) Limit(limit int) *PlayerFavoriteCardsQuery {
	pfcq.ctx.Limit = &limit
	return pfcq
}

// Offset to start from.
func (pfcq *PlayerFavoriteCardsQuery) Offset(offset int) *PlayerFavoriteCardsQuery {
	pfcq.ctx.Offset = &offset
	return pfcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pfcq *PlayerFavoriteCardsQuery) Unique(unique bool) *PlayerFavoriteCardsQuery {
	pfcq.ctx.Unique = &unique
	return pfcq
}

// Order specifies how the records should be ordered.
func (pfcq *PlayerFavoriteCardsQuery) Order(o ...playerfavoritecards.OrderOption) *PlayerFavoriteCardsQuery {
	pfcq.order = append(pfcq.order, o...)
	return pfcq
}

// QueryPlayer chains the current query on the "player" edge.
func (pfcq *PlayerFavoriteCardsQuery) QueryPlayer() *PlayerQuery {
	query := (&PlayerClient{config: pfcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pfcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pfcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(playerfavoritecards.Table, playerfavoritecards.PlayerColumn, selector),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, playerfavoritecards.PlayerTable, playerfavoritecards.PlayerColumn),
		)
		fromU = sqlgraph.SetNeighbors(pfcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCard chains the current query on the "card" edge.
func (pfcq *PlayerFavoriteCardsQuery) QueryCard() *CardQuery {
	query := (&CardClient{config: pfcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pfcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pfcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(playerfavoritecards.Table, playerfavoritecards.CardColumn, selector),
			sqlgraph.To(card.Table, card.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, playerfavoritecards.CardTable, playerfavoritecards.CardColumn),
		)
		fromU = sqlgraph.SetNeighbors(pfcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PlayerFavoriteCards entity from the query.
// Returns a *NotFoundError when no PlayerFavoriteCards was found.
func (pfcq *PlayerFavoriteCardsQuery) First(ctx context.Context) (*PlayerFavoriteCards, error) {
	nodes, err := pfcq.Limit(1).All(setContextOp(ctx, pfcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{playerfavoritecards.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pfcq *PlayerFavoriteCardsQuery) FirstX(ctx context.Context) *PlayerFavoriteCards {
	node, err := pfcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// Only returns a single PlayerFavoriteCards entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PlayerFavoriteCards entity is found.
// Returns a *NotFoundError when no PlayerFavoriteCards entities are found.
func (pfcq *PlayerFavoriteCardsQuery) Only(ctx context.Context) (*PlayerFavoriteCards, error) {
	nodes, err := pfcq.Limit(2).All(setContextOp(ctx, pfcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{playerfavoritecards.Label}
	default:
		return nil, &NotSingularError{playerfavoritecards.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pfcq *PlayerFavoriteCardsQuery) OnlyX(ctx context.Context) *PlayerFavoriteCards {
	node, err := pfcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// All executes the query and returns a list of PlayerFavoriteCardsSlice.
func (pfcq *PlayerFavoriteCardsQuery) All(ctx context.Context) ([]*PlayerFavoriteCards, error) {
	ctx = setContextOp(ctx, pfcq.ctx, ent.OpQueryAll)
	if err := pfcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PlayerFavoriteCards, *PlayerFavoriteCardsQuery]()
	return withInterceptors[[]*PlayerFavoriteCards](ctx, pfcq, qr, pfcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pfcq *PlayerFavoriteCardsQuery) AllX(ctx context.Context) []*PlayerFavoriteCards {
	nodes, err := pfcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// Count returns the count of the given query.
func (pfcq *PlayerFavoriteCardsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pfcq.ctx, ent.OpQueryCount)
	if err := pfcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pfcq, querierCount[*PlayerFavoriteCardsQuery](), pfcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pfcq *PlayerFavoriteCardsQuery) CountX(ctx context.Context) int {
	count, err := pfcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pfcq *PlayerFavoriteCardsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pfcq.ctx, ent.OpQueryExist)
	switch _, err := pfcq.First(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pfcq *PlayerFavoriteCardsQuery) ExistX(ctx context.Context) bool {
	exist, err := pfcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PlayerFavoriteCardsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pfcq *PlayerFavoriteCardsQuery) Clone() *PlayerFavoriteCardsQuery {
	if pfcq == nil {
		return nil
	}
	return &PlayerFavoriteCardsQuery{
		config:     pfcq.config,
		ctx:        pfcq.ctx.Clone(),
		order:      append([]playerfavoritecards.OrderOption{}, pfcq.order...),
		inters:     append([]Interceptor{}, pfcq.inters...),
		predicates: append([]predicate.PlayerFavoriteCards{}, pfcq.predicates...),
		withPlayer: pfcq.withPlayer.Clone(),
		withCard:   pfcq.withCard.Clone(),
		// clone intermediate query.
		sql:  pfcq.sql.Clone(),
		path: pfcq.path,
	}
}

// WithPlayer tells the query-builder to eager-load the nodes that are connected to
// the "player" edge. The optional arguments are used to configure the query builder of the edge.
func (pfcq *PlayerFavoriteCardsQuery) WithPlayer(opts ...func(*PlayerQuery)) *PlayerFavoriteCardsQuery {
	query := (&PlayerClient{config: pfcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pfcq.withPlayer = query
	return pfcq
}

// WithCard tells the query-builder to eager-load the nodes that are connected to
// the "card" edge. The optional arguments are used to configure the query builder of the edge.
func (pfcq *PlayerFavoriteCardsQuery) WithCard(opts ...func(*CardQuery)) *PlayerFavoriteCardsQuery {
	query := (&CardClient{config: pfcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pfcq.withCard = query
	return pfcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty,omitzero"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PlayerFavoriteCards.Query().
//		GroupBy(playerfavoritecards.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pfcq *PlayerFavoriteCardsQuery) GroupBy(field string, fields ...string) *PlayerFavoriteCardsGroupBy {
	pfcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PlayerFavoriteCardsGroupBy{build: pfcq}
	grbuild.flds = &pfcq.ctx.Fields
	grbuild.label = playerfavoritecards.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty,omitzero"`
//	}
//
//	client.PlayerFavoriteCards.Query().
//		Select(playerfavoritecards.FieldCreateTime).
//		Scan(ctx, &v)
func (pfcq *PlayerFavoriteCardsQuery) Select(fields ...string) *PlayerFavoriteCardsSelect {
	pfcq.ctx.Fields = append(pfcq.ctx.Fields, fields...)
	sbuild := &PlayerFavoriteCardsSelect{PlayerFavoriteCardsQuery: pfcq}
	sbuild.label = playerfavoritecards.Label
	sbuild.flds, sbuild.scan = &pfcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PlayerFavoriteCardsSelect configured with the given aggregations.
func (pfcq *PlayerFavoriteCardsQuery) Aggregate(fns ...AggregateFunc) *PlayerFavoriteCardsSelect {
	return pfcq.Select().Aggregate(fns...)
}

func (pfcq *PlayerFavoriteCardsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pfcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pfcq); err != nil {
				return err
			}
		}
	}
	for _, f := range pfcq.ctx.Fields {
		if !playerfavoritecards.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pfcq.path != nil {
		prev, err := pfcq.path(ctx)
		if err != nil {
			return err
		}
		pfcq.sql = prev
	}
	return nil
}

func (pfcq *PlayerFavoriteCardsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PlayerFavoriteCards, error) {
	var (
		nodes       = []*PlayerFavoriteCards{}
		_spec       = pfcq.querySpec()
		loadedTypes = [2]bool{
			pfcq.withPlayer != nil,
			pfcq.withCard != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PlayerFavoriteCards).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PlayerFavoriteCards{config: pfcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pfcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pfcq.withPlayer; query != nil {
		if err := pfcq.loadPlayer(ctx, query, nodes, nil,
			func(n *PlayerFavoriteCards, e *Player) { n.Edges.Player = e }); err != nil {
			return nil, err
		}
	}
	if query := pfcq.withCard; query != nil {
		if err := pfcq.loadCard(ctx, query, nodes, nil,
			func(n *PlayerFavoriteCards, e *Card) { n.Edges.Card = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pfcq *PlayerFavoriteCardsQuery) loadPlayer(ctx context.Context, query *PlayerQuery, nodes []*PlayerFavoriteCards, init func(*PlayerFavoriteCards), assign func(*PlayerFavoriteCards, *Player)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*PlayerFavoriteCards)
	for i := range nodes {
		fk := nodes[i].PlayerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(player.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "player_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (pfcq *PlayerFavoriteCardsQuery) loadCard(ctx context.Context, query *CardQuery, nodes []*PlayerFavoriteCards, init func(*PlayerFavoriteCards), assign func(*PlayerFavoriteCards, *Card)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*PlayerFavoriteCards)
	for i := range nodes {
		fk := nodes[i].CardID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(card.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "card_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pfcq *PlayerFavoriteCardsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pfcq.querySpec()
	_spec.Unique = false
	_spec.Node.Columns = nil
	return sqlgraph.CountNodes(ctx, pfcq.driver, _spec)
}

func (pfcq *PlayerFavoriteCardsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(playerfavoritecards.Table, playerfavoritecards.Columns, nil)
	_spec.From = pfcq.sql
	if unique := pfcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pfcq.path != nil {
		_spec.Unique = true
	}
	if fields := pfcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		for i := range fields {
			_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
		}
		if pfcq.withPlayer != nil {
			_spec.Node.AddColumnOnce(playerfavoritecards.FieldPlayerID)
		}
		if pfcq.withCard != nil {
			_spec.Node.AddColumnOnce(playerfavoritecards.FieldCardID)
		}
	}
	if ps := pfcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pfcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pfcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pfcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pfcq *PlayerFavoriteCardsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pfcq.driver.Dialect())
	t1 := builder.Table(playerfavoritecards.Table)
	columns := pfcq.ctx.Fields
	if len(columns) == 0 {
		columns = playerfavoritecards.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pfcq.sql != nil {
		selector = pfcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pfcq.ctx.Unique != nil && *pfcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pfcq.predicates {
		p(selector)
	}
	for _, p := range pfcq.order {
		p(selector)
	}
	if offset := pfcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pfcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PlayerFavoriteCardsGroupBy is the group-by builder for PlayerFavoriteCards entities.
type PlayerFavoriteCardsGroupBy struct {
	selector
	build *PlayerFavoriteCardsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pfcgb *PlayerFavoriteCardsGroupBy) Aggregate(fns ...AggregateFunc) *PlayerFavoriteCardsGroupBy {
	pfcgb.fns = append(pfcgb.fns, fns...)
	return pfcgb
}

// Scan applies the selector query and scans the result into the given value.
func (pfcgb *PlayerFavoriteCardsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pfcgb.build.ctx, ent.OpQueryGroupBy)
	if err := pfcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlayerFavoriteCardsQuery, *PlayerFavoriteCardsGroupBy](ctx, pfcgb.build, pfcgb, pfcgb.build.inters, v)
}

func (pfcgb *PlayerFavoriteCardsGroupBy) sqlScan(ctx context.Context, root *PlayerFavoriteCardsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pfcgb.fns))
	for _, fn := range pfcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pfcgb.flds)+len(pfcgb.fns))
		for _, f := range *pfcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pfcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pfcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PlayerFavoriteCardsSelect is the builder for selecting fields of PlayerFavoriteCards entities.
type PlayerFavoriteCardsSelect struct {
	*PlayerFavoriteCardsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pfcs *PlayerFavoriteCardsSelect) Aggregate(fns ...AggregateFunc) *PlayerFavoriteCardsSelect {
	pfcs.fns = append(pfcs.fns, fns...)
	return pfcs
}

// Scan applies the selector query and scans the result into the given value.
func (pfcs *PlayerFavoriteCardsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pfcs.ctx, ent.OpQuerySelect)
	if err := pfcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlayerFavoriteCardsQuery, *PlayerFavoriteCardsSelect](ctx, pfcs.PlayerFavoriteCardsQuery, pfcs, pfcs.inters, v)
}

func (pfcs *PlayerFavoriteCardsSelect) sqlScan(ctx context.Context, root *PlayerFavoriteCardsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pfcs.fns))
	for _, fn := range pfcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pfcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pfcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
