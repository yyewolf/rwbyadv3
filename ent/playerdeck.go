// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yyewolf/rwbyadv3/ent/card"
	"github.com/yyewolf/rwbyadv3/ent/player"
	"github.com/yyewolf/rwbyadv3/ent/playerdeck"
)

// PlayerDeck is the model entity for the PlayerDeck schema.
type PlayerDeck struct {
	config `json:"-"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// PlayerID holds the value of the "player_id" field.
	PlayerID string `json:"player_id,omitempty"`
	// CardID holds the value of the "card_id" field.
	CardID uuid.UUID `json:"card_id,omitempty"`
	// Position holds the value of the "position" field.
	Position float64 `json:"position,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PlayerDeckQuery when eager-loading is set.
	Edges        PlayerDeckEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PlayerDeckEdges holds the relations/edges for other nodes in the graph.
type PlayerDeckEdges struct {
	// Player holds the value of the player edge.
	Player *Player `json:"player,omitempty"`
	// Card holds the value of the card edge.
	Card *Card `json:"card,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// PlayerOrErr returns the Player value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PlayerDeckEdges) PlayerOrErr() (*Player, error) {
	if e.Player != nil {
		return e.Player, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: player.Label}
	}
	return nil, &NotLoadedError{edge: "player"}
}

// CardOrErr returns the Card value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PlayerDeckEdges) CardOrErr() (*Card, error) {
	if e.Card != nil {
		return e.Card, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: card.Label}
	}
	return nil, &NotLoadedError{edge: "card"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PlayerDeck) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case playerdeck.FieldPosition:
			values[i] = new(sql.NullFloat64)
		case playerdeck.FieldPlayerID:
			values[i] = new(sql.NullString)
		case playerdeck.FieldCreateTime, playerdeck.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case playerdeck.FieldCardID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PlayerDeck fields.
func (pd *PlayerDeck) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case playerdeck.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				pd.CreateTime = value.Time
			}
		case playerdeck.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				pd.UpdateTime = value.Time
			}
		case playerdeck.FieldPlayerID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field player_id", values[i])
			} else if value.Valid {
				pd.PlayerID = value.String
			}
		case playerdeck.FieldCardID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field card_id", values[i])
			} else if value != nil {
				pd.CardID = *value
			}
		case playerdeck.FieldPosition:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field position", values[i])
			} else if value.Valid {
				pd.Position = value.Float64
			}
		default:
			pd.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PlayerDeck.
// This includes values selected through modifiers, order, etc.
func (pd *PlayerDeck) Value(name string) (ent.Value, error) {
	return pd.selectValues.Get(name)
}

// QueryPlayer queries the "player" edge of the PlayerDeck entity.
func (pd *PlayerDeck) QueryPlayer() *PlayerQuery {
	return NewPlayerDeckClient(pd.config).QueryPlayer(pd)
}

// QueryCard queries the "card" edge of the PlayerDeck entity.
func (pd *PlayerDeck) QueryCard() *CardQuery {
	return NewPlayerDeckClient(pd.config).QueryCard(pd)
}

// Update returns a builder for updating this PlayerDeck.
// Note that you need to call PlayerDeck.Unwrap() before calling this method if this PlayerDeck
// was returned from a transaction, and the transaction was committed or rolled back.
func (pd *PlayerDeck) Update() *PlayerDeckUpdateOne {
	return NewPlayerDeckClient(pd.config).UpdateOne(pd)
}

// Unwrap unwraps the PlayerDeck entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pd *PlayerDeck) Unwrap() *PlayerDeck {
	_tx, ok := pd.config.driver.(*txDriver)
	if !ok {
		panic("ent: PlayerDeck is not a transactional entity")
	}
	pd.config.driver = _tx.drv
	return pd
}

// String implements the fmt.Stringer.
func (pd *PlayerDeck) String() string {
	var builder strings.Builder
	builder.WriteString("PlayerDeck(")
	builder.WriteString("create_time=")
	builder.WriteString(pd.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(pd.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("player_id=")
	builder.WriteString(pd.PlayerID)
	builder.WriteString(", ")
	builder.WriteString("card_id=")
	builder.WriteString(fmt.Sprintf("%v", pd.CardID))
	builder.WriteString(", ")
	builder.WriteString("position=")
	builder.WriteString(fmt.Sprintf("%v", pd.Position))
	builder.WriteByte(')')
	return builder.String()
}

// PlayerDecks is a parsable slice of PlayerDeck.
type PlayerDecks []*PlayerDeck
