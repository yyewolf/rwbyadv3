// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/yyewolf/rwbyadv3/ent/card"
	"github.com/yyewolf/rwbyadv3/ent/cardstats"
)

// CardStats is the model entity for the CardStats schema.
type CardStats struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty,omitzero"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty,omitzero"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty,omitzero"`
	// CardID holds the value of the "card_id" field.
	CardID uuid.UUID `json:"card_id,omitempty,omitzero"`
	// Health holds the value of the "health" field.
	Health int64 `json:"health,omitempty,omitzero"`
	// Armor holds the value of the "armor" field.
	Armor int64 `json:"armor,omitempty,omitzero"`
	// Damage holds the value of the "damage" field.
	Damage int64 `json:"damage,omitempty,omitzero"`
	// Healing holds the value of the "healing" field.
	Healing int64 `json:"healing,omitempty,omitzero"`
	// Speed holds the value of the "speed" field.
	Speed int64 `json:"speed,omitempty,omitzero"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CardStatsQuery when eager-loading is set.
	Edges        CardStatsEdges `json:"edges,omitempty"`
	selectValues sql.SelectValues
}

// CardStatsEdges holds the relations/edges for other nodes in the graph.
type CardStatsEdges struct {
	// Of holds the value of the of edge.
	Of *Card `json:"of,omitempty,omitzero"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OfOrErr returns the Of value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CardStatsEdges) OfOrErr() (*Card, error) {
	if e.Of != nil {
		return e.Of, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: card.Label}
	}
	return nil, &NotLoadedError{edge: "of"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CardStats) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case cardstats.FieldID, cardstats.FieldHealth, cardstats.FieldArmor, cardstats.FieldDamage, cardstats.FieldHealing, cardstats.FieldSpeed:
			values[i] = new(sql.NullInt64)
		case cardstats.FieldCreateTime, cardstats.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case cardstats.FieldCardID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CardStats fields.
func (cs *CardStats) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case cardstats.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			cs.ID = int(value.Int64)
		case cardstats.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				cs.CreateTime = value.Time
			}
		case cardstats.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				cs.UpdateTime = value.Time
			}
		case cardstats.FieldCardID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field card_id", values[i])
			} else if value != nil {
				cs.CardID = *value
			}
		case cardstats.FieldHealth:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field health", values[i])
			} else if value.Valid {
				cs.Health = value.Int64
			}
		case cardstats.FieldArmor:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field armor", values[i])
			} else if value.Valid {
				cs.Armor = value.Int64
			}
		case cardstats.FieldDamage:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field damage", values[i])
			} else if value.Valid {
				cs.Damage = value.Int64
			}
		case cardstats.FieldHealing:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field healing", values[i])
			} else if value.Valid {
				cs.Healing = value.Int64
			}
		case cardstats.FieldSpeed:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field speed", values[i])
			} else if value.Valid {
				cs.Speed = value.Int64
			}
		default:
			cs.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CardStats.
// This includes values selected through modifiers, order, etc.
func (cs *CardStats) Value(name string) (ent.Value, error) {
	return cs.selectValues.Get(name)
}

// QueryOf queries the "of" edge of the CardStats entity.
func (cs *CardStats) QueryOf() *CardQuery {
	return NewCardStatsClient(cs.config).QueryOf(cs)
}

// Update returns a builder for updating this CardStats.
// Note that you need to call CardStats.Unwrap() before calling this method if this CardStats
// was returned from a transaction, and the transaction was committed or rolled back.
func (cs *CardStats) Update() *CardStatsUpdateOne {
	return NewCardStatsClient(cs.config).UpdateOne(cs)
}

// Unwrap unwraps the CardStats entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cs *CardStats) Unwrap() *CardStats {
	_tx, ok := cs.config.driver.(*txDriver)
	if !ok {
		panic("ent: CardStats is not a transactional entity")
	}
	cs.config.driver = _tx.drv
	return cs
}

// String implements the fmt.Stringer.
func (cs *CardStats) String() string {
	var builder strings.Builder
	builder.WriteString("CardStats(")
	builder.WriteString(fmt.Sprintf("id=%v, ", cs.ID))
	builder.WriteString("create_time=")
	builder.WriteString(cs.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(cs.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("card_id=")
	builder.WriteString(fmt.Sprintf("%v", cs.CardID))
	builder.WriteString(", ")
	builder.WriteString("health=")
	builder.WriteString(fmt.Sprintf("%v", cs.Health))
	builder.WriteString(", ")
	builder.WriteString("armor=")
	builder.WriteString(fmt.Sprintf("%v", cs.Armor))
	builder.WriteString(", ")
	builder.WriteString("damage=")
	builder.WriteString(fmt.Sprintf("%v", cs.Damage))
	builder.WriteString(", ")
	builder.WriteString("healing=")
	builder.WriteString(fmt.Sprintf("%v", cs.Healing))
	builder.WriteString(", ")
	builder.WriteString("speed=")
	builder.WriteString(fmt.Sprintf("%v", cs.Speed))
	builder.WriteByte(')')
	return builder.String()
}

// CardStatsSlice is a parsable slice of CardStats.
type CardStatsSlice []*CardStats
