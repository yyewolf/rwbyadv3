// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// GithubStar is an object representing the database table.
type GithubStar struct {
	PlayerID     string      `boil:"player_id" json:"player_id" toml:"player_id" yaml:"player_id"`
	GithubUserID null.String `boil:"github_user_id" json:"github_user_id,omitempty" toml:"github_user_id" yaml:"github_user_id,omitempty"`
	HasStarred   bool        `boil:"has_starred" json:"has_starred" toml:"has_starred" yaml:"has_starred"`
	CreatedAt    time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt    time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt    null.Time   `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *githubStarR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L githubStarL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var GithubStarColumns = struct {
	PlayerID     string
	GithubUserID string
	HasStarred   string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
}{
	PlayerID:     "player_id",
	GithubUserID: "github_user_id",
	HasStarred:   "has_starred",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
	DeletedAt:    "deleted_at",
}

var GithubStarTableColumns = struct {
	PlayerID     string
	GithubUserID string
	HasStarred   string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
}{
	PlayerID:     "github_stars.player_id",
	GithubUserID: "github_stars.github_user_id",
	HasStarred:   "github_stars.has_starred",
	CreatedAt:    "github_stars.created_at",
	UpdatedAt:    "github_stars.updated_at",
	DeletedAt:    "github_stars.deleted_at",
}

// Generated where

var GithubStarWhere = struct {
	PlayerID     whereHelperstring
	GithubUserID whereHelpernull_String
	HasStarred   whereHelperbool
	CreatedAt    whereHelpertime_Time
	UpdatedAt    whereHelpertime_Time
	DeletedAt    whereHelpernull_Time
}{
	PlayerID:     whereHelperstring{field: "\"github_stars\".\"player_id\""},
	GithubUserID: whereHelpernull_String{field: "\"github_stars\".\"github_user_id\""},
	HasStarred:   whereHelperbool{field: "\"github_stars\".\"has_starred\""},
	CreatedAt:    whereHelpertime_Time{field: "\"github_stars\".\"created_at\""},
	UpdatedAt:    whereHelpertime_Time{field: "\"github_stars\".\"updated_at\""},
	DeletedAt:    whereHelpernull_Time{field: "\"github_stars\".\"deleted_at\""},
}

// GithubStarRels is where relationship names are stored.
var GithubStarRels = struct {
	Player   string
	IDPlayer string
}{
	Player:   "Player",
	IDPlayer: "IDPlayer",
}

// githubStarR is where relationships are stored.
type githubStarR struct {
	Player   *Player `boil:"Player" json:"Player" toml:"Player" yaml:"Player"`
	IDPlayer *Player `boil:"IDPlayer" json:"IDPlayer" toml:"IDPlayer" yaml:"IDPlayer"`
}

// NewStruct creates a new relationship struct
func (*githubStarR) NewStruct() *githubStarR {
	return &githubStarR{}
}

func (r *githubStarR) GetPlayer() *Player {
	if r == nil {
		return nil
	}
	return r.Player
}

func (r *githubStarR) GetIDPlayer() *Player {
	if r == nil {
		return nil
	}
	return r.IDPlayer
}

// githubStarL is where Load methods for each relationship are stored.
type githubStarL struct{}

var (
	githubStarAllColumns            = []string{"player_id", "github_user_id", "has_starred", "created_at", "updated_at", "deleted_at"}
	githubStarColumnsWithoutDefault = []string{"player_id"}
	githubStarColumnsWithDefault    = []string{"github_user_id", "has_starred", "created_at", "updated_at", "deleted_at"}
	githubStarPrimaryKeyColumns     = []string{"player_id"}
	githubStarGeneratedColumns      = []string{}
)

type (
	// GithubStarSlice is an alias for a slice of pointers to GithubStar.
	// This should almost always be used instead of []GithubStar.
	GithubStarSlice []*GithubStar
	// GithubStarHook is the signature for custom GithubStar hook methods
	GithubStarHook func(context.Context, boil.ContextExecutor, *GithubStar) error

	githubStarQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	githubStarType                 = reflect.TypeOf(&GithubStar{})
	githubStarMapping              = queries.MakeStructMapping(githubStarType)
	githubStarPrimaryKeyMapping, _ = queries.BindMapping(githubStarType, githubStarMapping, githubStarPrimaryKeyColumns)
	githubStarInsertCacheMut       sync.RWMutex
	githubStarInsertCache          = make(map[string]insertCache)
	githubStarUpdateCacheMut       sync.RWMutex
	githubStarUpdateCache          = make(map[string]updateCache)
	githubStarUpsertCacheMut       sync.RWMutex
	githubStarUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var githubStarAfterSelectMu sync.Mutex
var githubStarAfterSelectHooks []GithubStarHook

var githubStarBeforeInsertMu sync.Mutex
var githubStarBeforeInsertHooks []GithubStarHook
var githubStarAfterInsertMu sync.Mutex
var githubStarAfterInsertHooks []GithubStarHook

var githubStarBeforeUpdateMu sync.Mutex
var githubStarBeforeUpdateHooks []GithubStarHook
var githubStarAfterUpdateMu sync.Mutex
var githubStarAfterUpdateHooks []GithubStarHook

var githubStarBeforeDeleteMu sync.Mutex
var githubStarBeforeDeleteHooks []GithubStarHook
var githubStarAfterDeleteMu sync.Mutex
var githubStarAfterDeleteHooks []GithubStarHook

var githubStarBeforeUpsertMu sync.Mutex
var githubStarBeforeUpsertHooks []GithubStarHook
var githubStarAfterUpsertMu sync.Mutex
var githubStarAfterUpsertHooks []GithubStarHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *GithubStar) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range githubStarAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *GithubStar) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range githubStarBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *GithubStar) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range githubStarAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *GithubStar) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range githubStarBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *GithubStar) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range githubStarAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *GithubStar) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range githubStarBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *GithubStar) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range githubStarAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *GithubStar) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range githubStarBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *GithubStar) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range githubStarAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddGithubStarHook registers your hook function for all future operations.
func AddGithubStarHook(hookPoint boil.HookPoint, githubStarHook GithubStarHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		githubStarAfterSelectMu.Lock()
		githubStarAfterSelectHooks = append(githubStarAfterSelectHooks, githubStarHook)
		githubStarAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		githubStarBeforeInsertMu.Lock()
		githubStarBeforeInsertHooks = append(githubStarBeforeInsertHooks, githubStarHook)
		githubStarBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		githubStarAfterInsertMu.Lock()
		githubStarAfterInsertHooks = append(githubStarAfterInsertHooks, githubStarHook)
		githubStarAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		githubStarBeforeUpdateMu.Lock()
		githubStarBeforeUpdateHooks = append(githubStarBeforeUpdateHooks, githubStarHook)
		githubStarBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		githubStarAfterUpdateMu.Lock()
		githubStarAfterUpdateHooks = append(githubStarAfterUpdateHooks, githubStarHook)
		githubStarAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		githubStarBeforeDeleteMu.Lock()
		githubStarBeforeDeleteHooks = append(githubStarBeforeDeleteHooks, githubStarHook)
		githubStarBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		githubStarAfterDeleteMu.Lock()
		githubStarAfterDeleteHooks = append(githubStarAfterDeleteHooks, githubStarHook)
		githubStarAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		githubStarBeforeUpsertMu.Lock()
		githubStarBeforeUpsertHooks = append(githubStarBeforeUpsertHooks, githubStarHook)
		githubStarBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		githubStarAfterUpsertMu.Lock()
		githubStarAfterUpsertHooks = append(githubStarAfterUpsertHooks, githubStarHook)
		githubStarAfterUpsertMu.Unlock()
	}
}

// OneG returns a single githubStar record from the query using the global executor.
func (q githubStarQuery) OneG(ctx context.Context) (*GithubStar, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single githubStar record from the query.
func (q githubStarQuery) One(ctx context.Context, exec boil.ContextExecutor) (*GithubStar, error) {
	o := &GithubStar{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for github_stars")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all GithubStar records from the query using the global executor.
func (q githubStarQuery) AllG(ctx context.Context) (GithubStarSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all GithubStar records from the query.
func (q githubStarQuery) All(ctx context.Context, exec boil.ContextExecutor) (GithubStarSlice, error) {
	var o []*GithubStar

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to GithubStar slice")
	}

	if len(githubStarAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all GithubStar records in the query using the global executor
func (q githubStarQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all GithubStar records in the query.
func (q githubStarQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count github_stars rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q githubStarQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q githubStarQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if github_stars exists")
	}

	return count > 0, nil
}

// Player pointed to by the foreign key.
func (o *GithubStar) Player(mods ...qm.QueryMod) playerQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PlayerID),
	}

	queryMods = append(queryMods, mods...)

	return Players(queryMods...)
}

// IDPlayer pointed to by the foreign key.
func (o *GithubStar) IDPlayer(mods ...qm.QueryMod) playerQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PlayerID),
	}

	queryMods = append(queryMods, mods...)

	return Players(queryMods...)
}

// LoadPlayer allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (githubStarL) LoadPlayer(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGithubStar interface{}, mods queries.Applicator) error {
	var slice []*GithubStar
	var object *GithubStar

	if singular {
		var ok bool
		object, ok = maybeGithubStar.(*GithubStar)
		if !ok {
			object = new(GithubStar)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGithubStar)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGithubStar))
			}
		}
	} else {
		s, ok := maybeGithubStar.(*[]*GithubStar)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGithubStar)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGithubStar))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &githubStarR{}
		}
		args[object.PlayerID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &githubStarR{}
			}

			args[obj.PlayerID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`players`),
		qm.WhereIn(`players.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`players.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Player")
	}

	var resultSlice []*Player
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Player")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for players")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for players")
	}

	if len(playerAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Player = foreign
		if foreign.R == nil {
			foreign.R = &playerR{}
		}
		foreign.R.GithubStar = object
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PlayerID == foreign.ID {
				local.R.Player = foreign
				if foreign.R == nil {
					foreign.R = &playerR{}
				}
				foreign.R.GithubStar = local
				break
			}
		}
	}

	return nil
}

// LoadIDPlayer allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-1 relationship.
func (githubStarL) LoadIDPlayer(ctx context.Context, e boil.ContextExecutor, singular bool, maybeGithubStar interface{}, mods queries.Applicator) error {
	var slice []*GithubStar
	var object *GithubStar

	if singular {
		var ok bool
		object, ok = maybeGithubStar.(*GithubStar)
		if !ok {
			object = new(GithubStar)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeGithubStar)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeGithubStar))
			}
		}
	} else {
		s, ok := maybeGithubStar.(*[]*GithubStar)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeGithubStar)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeGithubStar))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &githubStarR{}
		}
		args[object.PlayerID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &githubStarR{}
			}

			args[obj.PlayerID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`players`),
		qm.WhereIn(`players.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`players.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Player")
	}

	var resultSlice []*Player
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Player")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for players")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for players")
	}

	if len(playerAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.IDPlayer = foreign
		if foreign.R == nil {
			foreign.R = &playerR{}
		}
		foreign.R.IDGithubStar = object
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PlayerID == foreign.ID {
				local.R.IDPlayer = foreign
				if foreign.R == nil {
					foreign.R = &playerR{}
				}
				foreign.R.IDGithubStar = local
				break
			}
		}
	}

	return nil
}

// SetPlayerG of the githubStar to the related item.
// Sets o.R.Player to related.
// Adds o to related.R.GithubStar.
// Uses the global database handle.
func (o *GithubStar) SetPlayerG(ctx context.Context, insert bool, related *Player) error {
	return o.SetPlayer(ctx, boil.GetContextDB(), insert, related)
}

// SetPlayer of the githubStar to the related item.
// Sets o.R.Player to related.
// Adds o to related.R.GithubStar.
func (o *GithubStar) SetPlayer(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Player) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"github_stars\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"player_id"}),
		strmangle.WhereClause("\"", "\"", 2, githubStarPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.PlayerID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PlayerID = related.ID
	if o.R == nil {
		o.R = &githubStarR{
			Player: related,
		}
	} else {
		o.R.Player = related
	}

	if related.R == nil {
		related.R = &playerR{
			GithubStar: o,
		}
	} else {
		related.R.GithubStar = o
	}

	return nil
}

// SetIDPlayerG of the githubStar to the related item.
// Sets o.R.IDPlayer to related.
// Adds o to related.R.IDGithubStar.
// Uses the global database handle.
func (o *GithubStar) SetIDPlayerG(ctx context.Context, insert bool, related *Player) error {
	return o.SetIDPlayer(ctx, boil.GetContextDB(), insert, related)
}

// SetIDPlayer of the githubStar to the related item.
// Sets o.R.IDPlayer to related.
// Adds o to related.R.IDGithubStar.
func (o *GithubStar) SetIDPlayer(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Player) error {
	var err error

	if insert {
		related.ID = o.PlayerID

		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	} else {
		updateQuery := fmt.Sprintf(
			"UPDATE \"players\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, []string{"id"}),
			strmangle.WhereClause("\"", "\"", 2, playerPrimaryKeyColumns),
		)
		values := []interface{}{o.PlayerID, related.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, updateQuery)
			fmt.Fprintln(writer, values)
		}
		if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
			return errors.Wrap(err, "failed to update foreign table")
		}

		related.ID = o.PlayerID
	}

	if o.R == nil {
		o.R = &githubStarR{
			IDPlayer: related,
		}
	} else {
		o.R.IDPlayer = related
	}

	if related.R == nil {
		related.R = &playerR{
			IDGithubStar: o,
		}
	} else {
		related.R.IDGithubStar = o
	}
	return nil
}

// GithubStars retrieves all the records using an executor.
func GithubStars(mods ...qm.QueryMod) githubStarQuery {
	mods = append(mods, qm.From("\"github_stars\""), qmhelper.WhereIsNull("\"github_stars\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"github_stars\".*"})
	}

	return githubStarQuery{q}
}

// FindGithubStarG retrieves a single record by ID.
func FindGithubStarG(ctx context.Context, playerID string, selectCols ...string) (*GithubStar, error) {
	return FindGithubStar(ctx, boil.GetContextDB(), playerID, selectCols...)
}

// FindGithubStar retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindGithubStar(ctx context.Context, exec boil.ContextExecutor, playerID string, selectCols ...string) (*GithubStar, error) {
	githubStarObj := &GithubStar{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"github_stars\" where \"player_id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, playerID)

	err := q.Bind(ctx, exec, githubStarObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from github_stars")
	}

	if err = githubStarObj.doAfterSelectHooks(ctx, exec); err != nil {
		return githubStarObj, err
	}

	return githubStarObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *GithubStar) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *GithubStar) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no github_stars provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(githubStarColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	githubStarInsertCacheMut.RLock()
	cache, cached := githubStarInsertCache[key]
	githubStarInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			githubStarAllColumns,
			githubStarColumnsWithDefault,
			githubStarColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(githubStarType, githubStarMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(githubStarType, githubStarMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"github_stars\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"github_stars\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into github_stars")
	}

	if !cached {
		githubStarInsertCacheMut.Lock()
		githubStarInsertCache[key] = cache
		githubStarInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single GithubStar record using the global executor.
// See Update for more documentation.
func (o *GithubStar) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the GithubStar.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *GithubStar) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	githubStarUpdateCacheMut.RLock()
	cache, cached := githubStarUpdateCache[key]
	githubStarUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			githubStarAllColumns,
			githubStarPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update github_stars, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"github_stars\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, githubStarPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(githubStarType, githubStarMapping, append(wl, githubStarPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update github_stars row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for github_stars")
	}

	if !cached {
		githubStarUpdateCacheMut.Lock()
		githubStarUpdateCache[key] = cache
		githubStarUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q githubStarQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q githubStarQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for github_stars")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for github_stars")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o GithubStarSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o GithubStarSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), githubStarPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"github_stars\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, githubStarPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in githubStar slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all githubStar")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *GithubStar) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *GithubStar) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no github_stars provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(githubStarColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	githubStarUpsertCacheMut.RLock()
	cache, cached := githubStarUpsertCache[key]
	githubStarUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			githubStarAllColumns,
			githubStarColumnsWithDefault,
			githubStarColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			githubStarAllColumns,
			githubStarPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert github_stars, could not build update column list")
		}

		ret := strmangle.SetComplement(githubStarAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(githubStarPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert github_stars, could not build conflict column list")
			}

			conflict = make([]string, len(githubStarPrimaryKeyColumns))
			copy(conflict, githubStarPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"github_stars\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(githubStarType, githubStarMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(githubStarType, githubStarMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert github_stars")
	}

	if !cached {
		githubStarUpsertCacheMut.Lock()
		githubStarUpsertCache[key] = cache
		githubStarUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single GithubStar record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *GithubStar) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// Delete deletes a single GithubStar record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *GithubStar) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no GithubStar provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), githubStarPrimaryKeyMapping)
		sql = "DELETE FROM \"github_stars\" WHERE \"player_id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"github_stars\" SET %s WHERE \"player_id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(githubStarType, githubStarMapping, append(wl, githubStarPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from github_stars")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for github_stars")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q githubStarQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAll deletes all matching rows.
func (q githubStarQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no githubStarQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from github_stars")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for github_stars")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o GithubStarSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o GithubStarSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(githubStarBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), githubStarPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"github_stars\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, githubStarPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), githubStarPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"github_stars\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, githubStarPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from githubStar slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for github_stars")
	}

	if len(githubStarAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *GithubStar) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no GithubStar provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *GithubStar) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindGithubStar(ctx, exec, o.PlayerID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *GithubStarSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty GithubStarSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *GithubStarSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := GithubStarSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), githubStarPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"github_stars\".* FROM \"github_stars\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, githubStarPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in GithubStarSlice")
	}

	*o = slice

	return nil
}

// GithubStarExistsG checks if the GithubStar row exists.
func GithubStarExistsG(ctx context.Context, playerID string) (bool, error) {
	return GithubStarExists(ctx, boil.GetContextDB(), playerID)
}

// GithubStarExists checks if the GithubStar row exists.
func GithubStarExists(ctx context.Context, exec boil.ContextExecutor, playerID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"github_stars\" where \"player_id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, playerID)
	}
	row := exec.QueryRowContext(ctx, sql, playerID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if github_stars exists")
	}

	return exists, nil
}

// Exists checks if the GithubStar row exists.
func (o *GithubStar) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return GithubStarExists(ctx, exec, o.PlayerID)
}
