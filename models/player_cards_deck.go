// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// PlayerCardsDeck is an object representing the database table.
type PlayerCardsDeck struct {
	PlayerID string `boil:"player_id" json:"player_id" toml:"player_id" yaml:"player_id"`
	CardID   string `boil:"card_id" json:"card_id" toml:"card_id" yaml:"card_id"`
	Position int    `boil:"position" json:"position" toml:"position" yaml:"position"`

	R *playerCardsDeckR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L playerCardsDeckL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PlayerCardsDeckColumns = struct {
	PlayerID string
	CardID   string
	Position string
}{
	PlayerID: "player_id",
	CardID:   "card_id",
	Position: "position",
}

var PlayerCardsDeckTableColumns = struct {
	PlayerID string
	CardID   string
	Position string
}{
	PlayerID: "player_cards_deck.player_id",
	CardID:   "player_cards_deck.card_id",
	Position: "player_cards_deck.position",
}

// Generated where

var PlayerCardsDeckWhere = struct {
	PlayerID whereHelperstring
	CardID   whereHelperstring
	Position whereHelperint
}{
	PlayerID: whereHelperstring{field: "\"player_cards_deck\".\"player_id\""},
	CardID:   whereHelperstring{field: "\"player_cards_deck\".\"card_id\""},
	Position: whereHelperint{field: "\"player_cards_deck\".\"position\""},
}

// PlayerCardsDeckRels is where relationship names are stored.
var PlayerCardsDeckRels = struct {
	Card   string
	Player string
}{
	Card:   "Card",
	Player: "Player",
}

// playerCardsDeckR is where relationships are stored.
type playerCardsDeckR struct {
	Card   *Card   `boil:"Card" json:"Card" toml:"Card" yaml:"Card"`
	Player *Player `boil:"Player" json:"Player" toml:"Player" yaml:"Player"`
}

// NewStruct creates a new relationship struct
func (*playerCardsDeckR) NewStruct() *playerCardsDeckR {
	return &playerCardsDeckR{}
}

func (r *playerCardsDeckR) GetCard() *Card {
	if r == nil {
		return nil
	}
	return r.Card
}

func (r *playerCardsDeckR) GetPlayer() *Player {
	if r == nil {
		return nil
	}
	return r.Player
}

// playerCardsDeckL is where Load methods for each relationship are stored.
type playerCardsDeckL struct{}

var (
	playerCardsDeckAllColumns            = []string{"player_id", "card_id", "position"}
	playerCardsDeckColumnsWithoutDefault = []string{"player_id", "card_id", "position"}
	playerCardsDeckColumnsWithDefault    = []string{}
	playerCardsDeckPrimaryKeyColumns     = []string{"player_id", "card_id"}
	playerCardsDeckGeneratedColumns      = []string{}
)

type (
	// PlayerCardsDeckSlice is an alias for a slice of pointers to PlayerCardsDeck.
	// This should almost always be used instead of []PlayerCardsDeck.
	PlayerCardsDeckSlice []*PlayerCardsDeck
	// PlayerCardsDeckHook is the signature for custom PlayerCardsDeck hook methods
	PlayerCardsDeckHook func(context.Context, boil.ContextExecutor, *PlayerCardsDeck) error

	playerCardsDeckQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	playerCardsDeckType                 = reflect.TypeOf(&PlayerCardsDeck{})
	playerCardsDeckMapping              = queries.MakeStructMapping(playerCardsDeckType)
	playerCardsDeckPrimaryKeyMapping, _ = queries.BindMapping(playerCardsDeckType, playerCardsDeckMapping, playerCardsDeckPrimaryKeyColumns)
	playerCardsDeckInsertCacheMut       sync.RWMutex
	playerCardsDeckInsertCache          = make(map[string]insertCache)
	playerCardsDeckUpdateCacheMut       sync.RWMutex
	playerCardsDeckUpdateCache          = make(map[string]updateCache)
	playerCardsDeckUpsertCacheMut       sync.RWMutex
	playerCardsDeckUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var playerCardsDeckAfterSelectMu sync.Mutex
var playerCardsDeckAfterSelectHooks []PlayerCardsDeckHook

var playerCardsDeckBeforeInsertMu sync.Mutex
var playerCardsDeckBeforeInsertHooks []PlayerCardsDeckHook
var playerCardsDeckAfterInsertMu sync.Mutex
var playerCardsDeckAfterInsertHooks []PlayerCardsDeckHook

var playerCardsDeckBeforeUpdateMu sync.Mutex
var playerCardsDeckBeforeUpdateHooks []PlayerCardsDeckHook
var playerCardsDeckAfterUpdateMu sync.Mutex
var playerCardsDeckAfterUpdateHooks []PlayerCardsDeckHook

var playerCardsDeckBeforeDeleteMu sync.Mutex
var playerCardsDeckBeforeDeleteHooks []PlayerCardsDeckHook
var playerCardsDeckAfterDeleteMu sync.Mutex
var playerCardsDeckAfterDeleteHooks []PlayerCardsDeckHook

var playerCardsDeckBeforeUpsertMu sync.Mutex
var playerCardsDeckBeforeUpsertHooks []PlayerCardsDeckHook
var playerCardsDeckAfterUpsertMu sync.Mutex
var playerCardsDeckAfterUpsertHooks []PlayerCardsDeckHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PlayerCardsDeck) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playerCardsDeckAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PlayerCardsDeck) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playerCardsDeckBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PlayerCardsDeck) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playerCardsDeckAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PlayerCardsDeck) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playerCardsDeckBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PlayerCardsDeck) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playerCardsDeckAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PlayerCardsDeck) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playerCardsDeckBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PlayerCardsDeck) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playerCardsDeckAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PlayerCardsDeck) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playerCardsDeckBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PlayerCardsDeck) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range playerCardsDeckAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPlayerCardsDeckHook registers your hook function for all future operations.
func AddPlayerCardsDeckHook(hookPoint boil.HookPoint, playerCardsDeckHook PlayerCardsDeckHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		playerCardsDeckAfterSelectMu.Lock()
		playerCardsDeckAfterSelectHooks = append(playerCardsDeckAfterSelectHooks, playerCardsDeckHook)
		playerCardsDeckAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		playerCardsDeckBeforeInsertMu.Lock()
		playerCardsDeckBeforeInsertHooks = append(playerCardsDeckBeforeInsertHooks, playerCardsDeckHook)
		playerCardsDeckBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		playerCardsDeckAfterInsertMu.Lock()
		playerCardsDeckAfterInsertHooks = append(playerCardsDeckAfterInsertHooks, playerCardsDeckHook)
		playerCardsDeckAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		playerCardsDeckBeforeUpdateMu.Lock()
		playerCardsDeckBeforeUpdateHooks = append(playerCardsDeckBeforeUpdateHooks, playerCardsDeckHook)
		playerCardsDeckBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		playerCardsDeckAfterUpdateMu.Lock()
		playerCardsDeckAfterUpdateHooks = append(playerCardsDeckAfterUpdateHooks, playerCardsDeckHook)
		playerCardsDeckAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		playerCardsDeckBeforeDeleteMu.Lock()
		playerCardsDeckBeforeDeleteHooks = append(playerCardsDeckBeforeDeleteHooks, playerCardsDeckHook)
		playerCardsDeckBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		playerCardsDeckAfterDeleteMu.Lock()
		playerCardsDeckAfterDeleteHooks = append(playerCardsDeckAfterDeleteHooks, playerCardsDeckHook)
		playerCardsDeckAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		playerCardsDeckBeforeUpsertMu.Lock()
		playerCardsDeckBeforeUpsertHooks = append(playerCardsDeckBeforeUpsertHooks, playerCardsDeckHook)
		playerCardsDeckBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		playerCardsDeckAfterUpsertMu.Lock()
		playerCardsDeckAfterUpsertHooks = append(playerCardsDeckAfterUpsertHooks, playerCardsDeckHook)
		playerCardsDeckAfterUpsertMu.Unlock()
	}
}

// OneG returns a single playerCardsDeck record from the query using the global executor.
func (q playerCardsDeckQuery) OneG(ctx context.Context) (*PlayerCardsDeck, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single playerCardsDeck record from the query.
func (q playerCardsDeckQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PlayerCardsDeck, error) {
	o := &PlayerCardsDeck{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for player_cards_deck")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all PlayerCardsDeck records from the query using the global executor.
func (q playerCardsDeckQuery) AllG(ctx context.Context) (PlayerCardsDeckSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all PlayerCardsDeck records from the query.
func (q playerCardsDeckQuery) All(ctx context.Context, exec boil.ContextExecutor) (PlayerCardsDeckSlice, error) {
	var o []*PlayerCardsDeck

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to PlayerCardsDeck slice")
	}

	if len(playerCardsDeckAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all PlayerCardsDeck records in the query using the global executor
func (q playerCardsDeckQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all PlayerCardsDeck records in the query.
func (q playerCardsDeckQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count player_cards_deck rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q playerCardsDeckQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q playerCardsDeckQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if player_cards_deck exists")
	}

	return count > 0, nil
}

// Card pointed to by the foreign key.
func (o *PlayerCardsDeck) Card(mods ...qm.QueryMod) cardQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CardID),
	}

	queryMods = append(queryMods, mods...)

	return Cards(queryMods...)
}

// Player pointed to by the foreign key.
func (o *PlayerCardsDeck) Player(mods ...qm.QueryMod) playerQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PlayerID),
	}

	queryMods = append(queryMods, mods...)

	return Players(queryMods...)
}

// LoadCard allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (playerCardsDeckL) LoadCard(ctx context.Context, e boil.ContextExecutor, singular bool, maybePlayerCardsDeck interface{}, mods queries.Applicator) error {
	var slice []*PlayerCardsDeck
	var object *PlayerCardsDeck

	if singular {
		var ok bool
		object, ok = maybePlayerCardsDeck.(*PlayerCardsDeck)
		if !ok {
			object = new(PlayerCardsDeck)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePlayerCardsDeck)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePlayerCardsDeck))
			}
		}
	} else {
		s, ok := maybePlayerCardsDeck.(*[]*PlayerCardsDeck)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePlayerCardsDeck)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePlayerCardsDeck))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &playerCardsDeckR{}
		}
		args[object.CardID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &playerCardsDeckR{}
			}

			args[obj.CardID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`cards`),
		qm.WhereIn(`cards.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`cards.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Card")
	}

	var resultSlice []*Card
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Card")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for cards")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for cards")
	}

	if len(cardAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Card = foreign
		if foreign.R == nil {
			foreign.R = &cardR{}
		}
		foreign.R.PlayerCardsDecks = append(foreign.R.PlayerCardsDecks, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CardID == foreign.ID {
				local.R.Card = foreign
				if foreign.R == nil {
					foreign.R = &cardR{}
				}
				foreign.R.PlayerCardsDecks = append(foreign.R.PlayerCardsDecks, local)
				break
			}
		}
	}

	return nil
}

// LoadPlayer allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (playerCardsDeckL) LoadPlayer(ctx context.Context, e boil.ContextExecutor, singular bool, maybePlayerCardsDeck interface{}, mods queries.Applicator) error {
	var slice []*PlayerCardsDeck
	var object *PlayerCardsDeck

	if singular {
		var ok bool
		object, ok = maybePlayerCardsDeck.(*PlayerCardsDeck)
		if !ok {
			object = new(PlayerCardsDeck)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePlayerCardsDeck)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePlayerCardsDeck))
			}
		}
	} else {
		s, ok := maybePlayerCardsDeck.(*[]*PlayerCardsDeck)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePlayerCardsDeck)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePlayerCardsDeck))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &playerCardsDeckR{}
		}
		args[object.PlayerID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &playerCardsDeckR{}
			}

			args[obj.PlayerID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`players`),
		qm.WhereIn(`players.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`players.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Player")
	}

	var resultSlice []*Player
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Player")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for players")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for players")
	}

	if len(playerAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Player = foreign
		if foreign.R == nil {
			foreign.R = &playerR{}
		}
		foreign.R.PlayerCardsDecks = append(foreign.R.PlayerCardsDecks, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PlayerID == foreign.ID {
				local.R.Player = foreign
				if foreign.R == nil {
					foreign.R = &playerR{}
				}
				foreign.R.PlayerCardsDecks = append(foreign.R.PlayerCardsDecks, local)
				break
			}
		}
	}

	return nil
}

// SetCardG of the playerCardsDeck to the related item.
// Sets o.R.Card to related.
// Adds o to related.R.PlayerCardsDecks.
// Uses the global database handle.
func (o *PlayerCardsDeck) SetCardG(ctx context.Context, insert bool, related *Card) error {
	return o.SetCard(ctx, boil.GetContextDB(), insert, related)
}

// SetCard of the playerCardsDeck to the related item.
// Sets o.R.Card to related.
// Adds o to related.R.PlayerCardsDecks.
func (o *PlayerCardsDeck) SetCard(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Card) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"player_cards_deck\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"card_id"}),
		strmangle.WhereClause("\"", "\"", 2, playerCardsDeckPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.PlayerID, o.CardID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CardID = related.ID
	if o.R == nil {
		o.R = &playerCardsDeckR{
			Card: related,
		}
	} else {
		o.R.Card = related
	}

	if related.R == nil {
		related.R = &cardR{
			PlayerCardsDecks: PlayerCardsDeckSlice{o},
		}
	} else {
		related.R.PlayerCardsDecks = append(related.R.PlayerCardsDecks, o)
	}

	return nil
}

// SetPlayerG of the playerCardsDeck to the related item.
// Sets o.R.Player to related.
// Adds o to related.R.PlayerCardsDecks.
// Uses the global database handle.
func (o *PlayerCardsDeck) SetPlayerG(ctx context.Context, insert bool, related *Player) error {
	return o.SetPlayer(ctx, boil.GetContextDB(), insert, related)
}

// SetPlayer of the playerCardsDeck to the related item.
// Sets o.R.Player to related.
// Adds o to related.R.PlayerCardsDecks.
func (o *PlayerCardsDeck) SetPlayer(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Player) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"player_cards_deck\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"player_id"}),
		strmangle.WhereClause("\"", "\"", 2, playerCardsDeckPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.PlayerID, o.CardID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PlayerID = related.ID
	if o.R == nil {
		o.R = &playerCardsDeckR{
			Player: related,
		}
	} else {
		o.R.Player = related
	}

	if related.R == nil {
		related.R = &playerR{
			PlayerCardsDecks: PlayerCardsDeckSlice{o},
		}
	} else {
		related.R.PlayerCardsDecks = append(related.R.PlayerCardsDecks, o)
	}

	return nil
}

// PlayerCardsDecks retrieves all the records using an executor.
func PlayerCardsDecks(mods ...qm.QueryMod) playerCardsDeckQuery {
	mods = append(mods, qm.From("\"player_cards_deck\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"player_cards_deck\".*"})
	}

	return playerCardsDeckQuery{q}
}

// FindPlayerCardsDeckG retrieves a single record by ID.
func FindPlayerCardsDeckG(ctx context.Context, playerID string, cardID string, selectCols ...string) (*PlayerCardsDeck, error) {
	return FindPlayerCardsDeck(ctx, boil.GetContextDB(), playerID, cardID, selectCols...)
}

// FindPlayerCardsDeck retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPlayerCardsDeck(ctx context.Context, exec boil.ContextExecutor, playerID string, cardID string, selectCols ...string) (*PlayerCardsDeck, error) {
	playerCardsDeckObj := &PlayerCardsDeck{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"player_cards_deck\" where \"player_id\"=$1 AND \"card_id\"=$2", sel,
	)

	q := queries.Raw(query, playerID, cardID)

	err := q.Bind(ctx, exec, playerCardsDeckObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from player_cards_deck")
	}

	if err = playerCardsDeckObj.doAfterSelectHooks(ctx, exec); err != nil {
		return playerCardsDeckObj, err
	}

	return playerCardsDeckObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *PlayerCardsDeck) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PlayerCardsDeck) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no player_cards_deck provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(playerCardsDeckColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	playerCardsDeckInsertCacheMut.RLock()
	cache, cached := playerCardsDeckInsertCache[key]
	playerCardsDeckInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			playerCardsDeckAllColumns,
			playerCardsDeckColumnsWithDefault,
			playerCardsDeckColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(playerCardsDeckType, playerCardsDeckMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(playerCardsDeckType, playerCardsDeckMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"player_cards_deck\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"player_cards_deck\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into player_cards_deck")
	}

	if !cached {
		playerCardsDeckInsertCacheMut.Lock()
		playerCardsDeckInsertCache[key] = cache
		playerCardsDeckInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single PlayerCardsDeck record using the global executor.
// See Update for more documentation.
func (o *PlayerCardsDeck) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the PlayerCardsDeck.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PlayerCardsDeck) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	playerCardsDeckUpdateCacheMut.RLock()
	cache, cached := playerCardsDeckUpdateCache[key]
	playerCardsDeckUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			playerCardsDeckAllColumns,
			playerCardsDeckPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update player_cards_deck, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"player_cards_deck\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, playerCardsDeckPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(playerCardsDeckType, playerCardsDeckMapping, append(wl, playerCardsDeckPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update player_cards_deck row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for player_cards_deck")
	}

	if !cached {
		playerCardsDeckUpdateCacheMut.Lock()
		playerCardsDeckUpdateCache[key] = cache
		playerCardsDeckUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q playerCardsDeckQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q playerCardsDeckQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for player_cards_deck")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for player_cards_deck")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o PlayerCardsDeckSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PlayerCardsDeckSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), playerCardsDeckPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"player_cards_deck\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, playerCardsDeckPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in playerCardsDeck slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all playerCardsDeck")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *PlayerCardsDeck) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PlayerCardsDeck) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no player_cards_deck provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(playerCardsDeckColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	playerCardsDeckUpsertCacheMut.RLock()
	cache, cached := playerCardsDeckUpsertCache[key]
	playerCardsDeckUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			playerCardsDeckAllColumns,
			playerCardsDeckColumnsWithDefault,
			playerCardsDeckColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			playerCardsDeckAllColumns,
			playerCardsDeckPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert player_cards_deck, could not build update column list")
		}

		ret := strmangle.SetComplement(playerCardsDeckAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(playerCardsDeckPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert player_cards_deck, could not build conflict column list")
			}

			conflict = make([]string, len(playerCardsDeckPrimaryKeyColumns))
			copy(conflict, playerCardsDeckPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"player_cards_deck\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(playerCardsDeckType, playerCardsDeckMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(playerCardsDeckType, playerCardsDeckMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert player_cards_deck")
	}

	if !cached {
		playerCardsDeckUpsertCacheMut.Lock()
		playerCardsDeckUpsertCache[key] = cache
		playerCardsDeckUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single PlayerCardsDeck record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *PlayerCardsDeck) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single PlayerCardsDeck record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PlayerCardsDeck) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no PlayerCardsDeck provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), playerCardsDeckPrimaryKeyMapping)
	sql := "DELETE FROM \"player_cards_deck\" WHERE \"player_id\"=$1 AND \"card_id\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from player_cards_deck")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for player_cards_deck")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q playerCardsDeckQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q playerCardsDeckQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no playerCardsDeckQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from player_cards_deck")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for player_cards_deck")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o PlayerCardsDeckSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PlayerCardsDeckSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(playerCardsDeckBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), playerCardsDeckPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"player_cards_deck\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, playerCardsDeckPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from playerCardsDeck slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for player_cards_deck")
	}

	if len(playerCardsDeckAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *PlayerCardsDeck) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no PlayerCardsDeck provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PlayerCardsDeck) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPlayerCardsDeck(ctx, exec, o.PlayerID, o.CardID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PlayerCardsDeckSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty PlayerCardsDeckSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PlayerCardsDeckSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PlayerCardsDeckSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), playerCardsDeckPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"player_cards_deck\".* FROM \"player_cards_deck\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, playerCardsDeckPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in PlayerCardsDeckSlice")
	}

	*o = slice

	return nil
}

// PlayerCardsDeckExistsG checks if the PlayerCardsDeck row exists.
func PlayerCardsDeckExistsG(ctx context.Context, playerID string, cardID string) (bool, error) {
	return PlayerCardsDeckExists(ctx, boil.GetContextDB(), playerID, cardID)
}

// PlayerCardsDeckExists checks if the PlayerCardsDeck row exists.
func PlayerCardsDeckExists(ctx context.Context, exec boil.ContextExecutor, playerID string, cardID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"player_cards_deck\" where \"player_id\"=$1 AND \"card_id\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, playerID, cardID)
	}
	row := exec.QueryRowContext(ctx, sql, playerID, cardID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if player_cards_deck exists")
	}

	return exists, nil
}

// Exists checks if the PlayerCardsDeck row exists.
func (o *PlayerCardsDeck) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PlayerCardsDeckExists(ctx, exec, o.PlayerID, o.CardID)
}
