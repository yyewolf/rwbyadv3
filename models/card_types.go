// Code generated by SQLBoiler 4.19.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CardType is an object representing the database table.
type CardType struct {
	CardType   string `boil:"card_type" json:"card_type" toml:"card_type" yaml:"card_type"`
	Name       string `boil:"name" json:"name" toml:"name" yaml:"name"`
	Categories string `boil:"categories" json:"categories" toml:"categories" yaml:"categories"`

	R *cardTypeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cardTypeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CardTypeColumns = struct {
	CardType   string
	Name       string
	Categories string
}{
	CardType:   "card_type",
	Name:       "name",
	Categories: "categories",
}

var CardTypeTableColumns = struct {
	CardType   string
	Name       string
	Categories string
}{
	CardType:   "card_types.card_type",
	Name:       "card_types.name",
	Categories: "card_types.categories",
}

// Generated where

var CardTypeWhere = struct {
	CardType   whereHelperstring
	Name       whereHelperstring
	Categories whereHelperstring
}{
	CardType:   whereHelperstring{field: "\"card_types\".\"card_type\""},
	Name:       whereHelperstring{field: "\"card_types\".\"name\""},
	Categories: whereHelperstring{field: "\"card_types\".\"categories\""},
}

// CardTypeRels is where relationship names are stored.
var CardTypeRels = struct {
}{}

// cardTypeR is where relationships are stored.
type cardTypeR struct {
}

// NewStruct creates a new relationship struct
func (*cardTypeR) NewStruct() *cardTypeR {
	return &cardTypeR{}
}

// cardTypeL is where Load methods for each relationship are stored.
type cardTypeL struct{}

var (
	cardTypeAllColumns            = []string{"card_type", "name", "categories"}
	cardTypeColumnsWithoutDefault = []string{"card_type", "name", "categories"}
	cardTypeColumnsWithDefault    = []string{}
	cardTypePrimaryKeyColumns     = []string{"card_type"}
	cardTypeGeneratedColumns      = []string{}
)

type (
	// CardTypeSlice is an alias for a slice of pointers to CardType.
	// This should almost always be used instead of []CardType.
	CardTypeSlice []*CardType
	// CardTypeHook is the signature for custom CardType hook methods
	CardTypeHook func(context.Context, boil.ContextExecutor, *CardType) error

	cardTypeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cardTypeType                 = reflect.TypeOf(&CardType{})
	cardTypeMapping              = queries.MakeStructMapping(cardTypeType)
	cardTypePrimaryKeyMapping, _ = queries.BindMapping(cardTypeType, cardTypeMapping, cardTypePrimaryKeyColumns)
	cardTypeInsertCacheMut       sync.RWMutex
	cardTypeInsertCache          = make(map[string]insertCache)
	cardTypeUpdateCacheMut       sync.RWMutex
	cardTypeUpdateCache          = make(map[string]updateCache)
	cardTypeUpsertCacheMut       sync.RWMutex
	cardTypeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var cardTypeAfterSelectMu sync.Mutex
var cardTypeAfterSelectHooks []CardTypeHook

var cardTypeBeforeInsertMu sync.Mutex
var cardTypeBeforeInsertHooks []CardTypeHook
var cardTypeAfterInsertMu sync.Mutex
var cardTypeAfterInsertHooks []CardTypeHook

var cardTypeBeforeUpdateMu sync.Mutex
var cardTypeBeforeUpdateHooks []CardTypeHook
var cardTypeAfterUpdateMu sync.Mutex
var cardTypeAfterUpdateHooks []CardTypeHook

var cardTypeBeforeDeleteMu sync.Mutex
var cardTypeBeforeDeleteHooks []CardTypeHook
var cardTypeAfterDeleteMu sync.Mutex
var cardTypeAfterDeleteHooks []CardTypeHook

var cardTypeBeforeUpsertMu sync.Mutex
var cardTypeBeforeUpsertHooks []CardTypeHook
var cardTypeAfterUpsertMu sync.Mutex
var cardTypeAfterUpsertHooks []CardTypeHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CardType) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cardTypeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CardType) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cardTypeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CardType) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cardTypeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CardType) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cardTypeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CardType) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cardTypeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CardType) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cardTypeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CardType) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cardTypeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CardType) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cardTypeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CardType) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cardTypeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCardTypeHook registers your hook function for all future operations.
func AddCardTypeHook(hookPoint boil.HookPoint, cardTypeHook CardTypeHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		cardTypeAfterSelectMu.Lock()
		cardTypeAfterSelectHooks = append(cardTypeAfterSelectHooks, cardTypeHook)
		cardTypeAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		cardTypeBeforeInsertMu.Lock()
		cardTypeBeforeInsertHooks = append(cardTypeBeforeInsertHooks, cardTypeHook)
		cardTypeBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		cardTypeAfterInsertMu.Lock()
		cardTypeAfterInsertHooks = append(cardTypeAfterInsertHooks, cardTypeHook)
		cardTypeAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		cardTypeBeforeUpdateMu.Lock()
		cardTypeBeforeUpdateHooks = append(cardTypeBeforeUpdateHooks, cardTypeHook)
		cardTypeBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		cardTypeAfterUpdateMu.Lock()
		cardTypeAfterUpdateHooks = append(cardTypeAfterUpdateHooks, cardTypeHook)
		cardTypeAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		cardTypeBeforeDeleteMu.Lock()
		cardTypeBeforeDeleteHooks = append(cardTypeBeforeDeleteHooks, cardTypeHook)
		cardTypeBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		cardTypeAfterDeleteMu.Lock()
		cardTypeAfterDeleteHooks = append(cardTypeAfterDeleteHooks, cardTypeHook)
		cardTypeAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		cardTypeBeforeUpsertMu.Lock()
		cardTypeBeforeUpsertHooks = append(cardTypeBeforeUpsertHooks, cardTypeHook)
		cardTypeBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		cardTypeAfterUpsertMu.Lock()
		cardTypeAfterUpsertHooks = append(cardTypeAfterUpsertHooks, cardTypeHook)
		cardTypeAfterUpsertMu.Unlock()
	}
}

// OneG returns a single cardType record from the query using the global executor.
func (q cardTypeQuery) OneG(ctx context.Context) (*CardType, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single cardType record from the query.
func (q cardTypeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CardType, error) {
	o := &CardType{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for card_types")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all CardType records from the query using the global executor.
func (q cardTypeQuery) AllG(ctx context.Context) (CardTypeSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all CardType records from the query.
func (q cardTypeQuery) All(ctx context.Context, exec boil.ContextExecutor) (CardTypeSlice, error) {
	var o []*CardType

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CardType slice")
	}

	if len(cardTypeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all CardType records in the query using the global executor
func (q cardTypeQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all CardType records in the query.
func (q cardTypeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count card_types rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q cardTypeQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q cardTypeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if card_types exists")
	}

	return count > 0, nil
}

// CardTypes retrieves all the records using an executor.
func CardTypes(mods ...qm.QueryMod) cardTypeQuery {
	mods = append(mods, qm.From("\"card_types\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"card_types\".*"})
	}

	return cardTypeQuery{q}
}

// FindCardTypeG retrieves a single record by ID.
func FindCardTypeG(ctx context.Context, cardType string, selectCols ...string) (*CardType, error) {
	return FindCardType(ctx, boil.GetContextDB(), cardType, selectCols...)
}

// FindCardType retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCardType(ctx context.Context, exec boil.ContextExecutor, cardType string, selectCols ...string) (*CardType, error) {
	cardTypeObj := &CardType{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"card_types\" where \"card_type\"=$1", sel,
	)

	q := queries.Raw(query, cardType)

	err := q.Bind(ctx, exec, cardTypeObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from card_types")
	}

	if err = cardTypeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return cardTypeObj, err
	}

	return cardTypeObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *CardType) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CardType) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no card_types provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cardTypeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cardTypeInsertCacheMut.RLock()
	cache, cached := cardTypeInsertCache[key]
	cardTypeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cardTypeAllColumns,
			cardTypeColumnsWithDefault,
			cardTypeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cardTypeType, cardTypeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cardTypeType, cardTypeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"card_types\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"card_types\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into card_types")
	}

	if !cached {
		cardTypeInsertCacheMut.Lock()
		cardTypeInsertCache[key] = cache
		cardTypeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single CardType record using the global executor.
// See Update for more documentation.
func (o *CardType) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the CardType.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CardType) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	cardTypeUpdateCacheMut.RLock()
	cache, cached := cardTypeUpdateCache[key]
	cardTypeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cardTypeAllColumns,
			cardTypePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update card_types, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"card_types\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, cardTypePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cardTypeType, cardTypeMapping, append(wl, cardTypePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update card_types row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for card_types")
	}

	if !cached {
		cardTypeUpdateCacheMut.Lock()
		cardTypeUpdateCache[key] = cache
		cardTypeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q cardTypeQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q cardTypeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for card_types")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for card_types")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o CardTypeSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CardTypeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cardTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"card_types\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, cardTypePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cardType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cardType")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *CardType) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CardType) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no card_types provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cardTypeColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cardTypeUpsertCacheMut.RLock()
	cache, cached := cardTypeUpsertCache[key]
	cardTypeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			cardTypeAllColumns,
			cardTypeColumnsWithDefault,
			cardTypeColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			cardTypeAllColumns,
			cardTypePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert card_types, could not build update column list")
		}

		ret := strmangle.SetComplement(cardTypeAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(cardTypePrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert card_types, could not build conflict column list")
			}

			conflict = make([]string, len(cardTypePrimaryKeyColumns))
			copy(conflict, cardTypePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"card_types\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(cardTypeType, cardTypeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cardTypeType, cardTypeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert card_types")
	}

	if !cached {
		cardTypeUpsertCacheMut.Lock()
		cardTypeUpsertCache[key] = cache
		cardTypeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single CardType record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *CardType) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single CardType record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CardType) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CardType provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cardTypePrimaryKeyMapping)
	sql := "DELETE FROM \"card_types\" WHERE \"card_type\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from card_types")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for card_types")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q cardTypeQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q cardTypeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cardTypeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from card_types")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for card_types")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o CardTypeSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CardTypeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(cardTypeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cardTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"card_types\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, cardTypePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cardType slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for card_types")
	}

	if len(cardTypeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *CardType) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no CardType provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CardType) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCardType(ctx, exec, o.CardType)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CardTypeSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty CardTypeSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CardTypeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CardTypeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cardTypePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"card_types\".* FROM \"card_types\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, cardTypePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CardTypeSlice")
	}

	*o = slice

	return nil
}

// CardTypeExistsG checks if the CardType row exists.
func CardTypeExistsG(ctx context.Context, cardType string) (bool, error) {
	return CardTypeExists(ctx, boil.GetContextDB(), cardType)
}

// CardTypeExists checks if the CardType row exists.
func CardTypeExists(ctx context.Context, exec boil.ContextExecutor, cardType string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"card_types\" where \"card_type\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, cardType)
	}
	row := exec.QueryRowContext(ctx, sql, cardType)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if card_types exists")
	}

	return exists, nil
}

// Exists checks if the CardType row exists.
func (o *CardType) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CardTypeExists(ctx, exec, o.CardType)
}
